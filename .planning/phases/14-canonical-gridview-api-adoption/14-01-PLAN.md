---
phase: 14-canonical-gridview-api-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/grid-view.ts
  - packages/rts-engine/grid-view.test.ts
  - packages/rts-engine/placement-transform.ts
  - packages/rts-engine/placement-transform.test.ts
  - packages/rts-engine/index.ts
autonomous: true
requirements:
  - REF-02
must_haves:
  truths:
    - 'Engine callers can chain `GridView.translate`, `rotate`, `flipHorizontal`, `flipVertical`, and `applyTransform` without mutating prior views.'
    - 'Transform chaining remains order-sensitive and deterministic for identical input sequences across runs and runtimes.'
    - '`GridView.applyTransform` rejects out-of-contract matrices with explicit, actionable errors instead of silently coercing values.'
  artifacts:
    - path: packages/rts-engine/grid-view.ts
      provides: Canonical immutable GridView transform API
      contains: translate/rotate/flip/applyTransform operations, placement-safe matrix guards, deterministic cell remapping
    - path: packages/rts-engine/grid-view.test.ts
      provides: Contract and parity regression tests for GridView transforms
      contains: rotate-cycle parity, call-order semantics, immutability, invalid-matrix rejection assertions
    - path: packages/rts-engine/placement-transform.ts
      provides: Shared transform helper bridge for parity safety
      contains: matrix helper reuse points and compatibility coverage hooks consumed by GridView tests
    - path: packages/rts-engine/placement-transform.test.ts
      provides: Transform-equivalence regression coverage
      contains: fixture comparisons between placement-transform outputs and GridView operations
  key_links:
    - from: packages/rts-engine/grid-view.test.ts
      to: packages/rts-engine/grid-view.ts
      via: direct API assertions against transform chaining and validation behavior
      pattern: translate|rotate|flipHorizontal|flipVertical|applyTransform
    - from: packages/rts-engine/placement-transform.test.ts
      to: packages/rts-engine/placement-transform.ts
      via: matrix-fixture parity checks used as transform semantic baseline
      pattern: normalizePlacementTransform|projectTemplateWithTransform
---

<objective>
Implement the canonical GridView transform API so engine geometry consumers can use one immutable, deterministic transform surface with strict placement-safe matrix validation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-canonical-gridview-api-adoption/14-CONTEXT.md
@.planning/phases/13-gridview-contract-and-cell-semantics/13-01-SUMMARY.md
@packages/rts-engine/grid-view.ts
@packages/rts-engine/grid-view.test.ts
@packages/rts-engine/placement-transform.ts
@packages/rts-engine/placement-transform.test.ts
</context>

<tasks>
  <task type="auto">
    <name>Add immutable GridView transform operations</name>
    <files>
      - packages/rts-engine/grid-view.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Extend `GridView` with immutable transform methods: `translate(dx, dy)`, `rotate(times?)`, `flipHorizontal()`, `flipVertical()`, and `applyTransform(matrix)` (with optional `applyMatrix` alias if needed for caller ergonomics).
      Keep `cells()` semantics from Phase 13 unchanged: deterministic ordering, full alive/dead coverage, no duplicate coordinates.
      Ensure each transform returns a new GridView instance and never mutates previous instances.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/grid-view.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      GridView exposes chainable immutable transform methods with deterministic output and package exports remain stable.
    </done>
  </task>

  <task type="auto">
    <name>Enforce placement-safe matrix validation for applyTransform</name>
    <files>
      - packages/rts-engine/grid-view.ts
      - packages/rts-engine/placement-transform.ts
    </files>
    <action>
      Restrict `applyTransform` inputs to placement-safe integer orthogonal matrices compatible with current authoritative placement semantics.
      Reject invalid matrix inputs with explicit fail-fast errors that describe accepted transform contract and migration guidance.
      Reuse or share placement-transform helper logic where appropriate to avoid transform-semantic drift.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/grid-view.test.ts packages/rts-engine/placement-transform.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Invalid matrices fail fast with actionable errors, while valid matrices preserve existing placement transform semantics.
    </done>
  </task>

  <task type="auto">
    <name>Add transform parity and determinism regression tests</name>
    <files>
      - packages/rts-engine/grid-view.test.ts
      - packages/rts-engine/placement-transform.test.ts
    </files>
    <action>
      Add fixture-driven parity assertions proving GridView transform outputs match existing placement-transform behavior for representative rotate/flip operation sequences.
      Include chaining-order assertions and four-rotate identity-cycle checks on asymmetric templates.
      Add regression checks confirming repeated identical transform chains produce identical ordered cell output.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/grid-view.test.ts packages/rts-engine/placement-transform.test.ts
        - npm run test:unit
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      REF-02 parity expectations are test-covered and fail on transform-semantics drift.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/grid-view.test.ts packages/rts-engine/placement-transform.test.ts`
- `npm run test:unit`

Confirm:

- GridView transform operations are immutable, deterministic, and order-sensitive
- applyTransform rejects out-of-contract matrices with explicit guidance
- parity fixtures match existing placement-transform semantics
  </verification>

<success_criteria>

- Phase 14 requirement `REF-02` has a canonical tested GridView transform API.
- Transform semantics remain parity-equivalent with current rotate/flip behavior.
- Invalid transform input handling is explicit and fail-fast.
  </success_criteria>

<output>
Create `.planning/phases/14-canonical-gridview-api-adoption/14-01-SUMMARY.md` with implementation details, verification evidence, and any migration notes.
</output>
