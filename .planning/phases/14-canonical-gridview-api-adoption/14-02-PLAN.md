---
phase: 14-canonical-gridview-api-adoption
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - packages/rts-engine/placement-transform.ts
  - packages/rts-engine/placement-transform.test.ts
  - packages/rts-engine/index.ts
autonomous: true
requirements:
  - REF-01
  - REF-02
must_haves:
  truths:
    - 'Templates expose `grid()` as the canonical transformable geometry entrypoint and repeated calls return equivalent fresh GridView instances.'
    - 'Engine geometry acquisition paths use `template.grid()` plus GridView transforms instead of legacy offset-template entrypoints.'
    - 'Legacy entrypoint misuse fails fast with actionable migration errors while equivalent preview/queue inputs keep existing behavior.'
  artifacts:
    - path: packages/rts-engine/rts.ts
      provides: Canonical template grid entrypoint wiring and engine caller cutover
      contains: template normalization with grid(), canonical geometry acquisition helpers, caller migration to GridView transforms
    - path: packages/rts-engine/placement-transform.ts
      provides: Legacy-entrypoint migration guardrails
      contains: compatibility bridge/fail-fast handling with actionable migration guidance
    - path: packages/rts-engine/rts.test.ts
      provides: Engine parity and template.grid regression coverage
      contains: repeated template.grid determinism checks and preview/queue transform parity assertions
    - path: packages/rts-engine/placement-transform.test.ts
      provides: Migration guardrail regression tests
      contains: legacy-entrypoint failure expectations and canonical API equivalence checks
    - path: packages/rts-engine/index.ts
      provides: Canonical package API exports
      contains: template/grid-facing exports and legacy export cleanup
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/grid-view.ts
      via: canonical transform chain for template geometry acquisition
      pattern: grid\(\)|applyTransform|translate
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: preview and queue assertions for transform parity outcomes
      pattern: previewBuildPlacement|queueBuildEvent|createRoomState
    - from: packages/rts-engine/placement-transform.test.ts
      to: packages/rts-engine/placement-transform.ts
      via: fail-fast error and migration-guidance assertions on legacy entrypoints
      pattern: projectTemplateWithTransform|projectPlacementToWorld
---

<objective>
Adopt `template.grid()` as the canonical engine template entrypoint, migrate engine callers to GridView-based geometry acquisition, and retire legacy offset-template entrypoints with fail-fast migration guidance.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-canonical-gridview-api-adoption/14-CONTEXT.md
@.planning/phases/14-canonical-gridview-api-adoption/14-01-PLAN.md
@packages/rts-engine/rts.ts
@packages/rts-engine/rts.test.ts
@packages/rts-engine/placement-transform.ts
@packages/rts-engine/placement-transform.test.ts
</context>

<tasks>
  <task type="auto">
    <name>Normalize templates with canonical grid() entrypoint</name>
    <files>
      - packages/rts-engine/rts.ts
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Add template normalization so every runtime template (default and injected) exposes `grid(): GridView` as the canonical transformable shape entrypoint.
      Ensure `grid()` returns equivalent fresh immutable views on repeated calls to avoid shared mutable aliasing.
      Keep template anchor/pivot semantics unchanged for existing placements.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      REF-01 entrypoint exists across template creation paths and is regression-tested for deterministic repeated calls.
    </done>
  </task>

  <task type="auto">
    <name>Cut over engine geometry acquisition to template.grid transforms</name>
    <files>
      - packages/rts-engine/rts.ts
      - packages/rts-engine/rts.test.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Replace engine callsites that obtain transformed template geometry from legacy offset-template entrypoints so they start from `template.grid()` and GridView transform operations.
      Preserve existing preview/queue transform results for equivalent inputs (bounds, footprint, illegal cells, rejection reasons).
      Keep downstream behavior stable by adapting canonical GridView outputs into current callsite expectations where required.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Engine callers no longer depend on legacy transformed-template entrypoints for geometry acquisition.
    </done>
  </task>

  <task type="auto">
    <name>Retire legacy offset-template entrypoints with fail-fast migration errors</name>
    <files>
      - packages/rts-engine/placement-transform.ts
      - packages/rts-engine/placement-transform.test.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Remove legacy offset-template projection entrypoints from canonical usage; any remaining direct legacy calls should fail fast with actionable migration messages pointing to `template.grid()` and GridView transform methods.
      Preserve a short migration bridge only if required by in-repo callsites during this plan, but finish with fail-fast behavior for deprecated external entrypoint usage by plan completion.
      Keep placement-transform normalization APIs used by payload contract handling intact.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/placement-transform.test.ts packages/rts-engine/rts.test.ts
        - npm run test:unit
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Legacy entrypoints are retired from canonical use and misuse returns explicit migration guidance without behavior drift.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/placement-transform.test.ts`
- `npm run test:unit`

Confirm:

- every template exposes canonical `grid()` entrypoint
- engine transform geometry acquisition originates from `template.grid()` + GridView transforms
- legacy entrypoint misuse fails fast with actionable migration guidance
- preview/queue parity for equivalent inputs remains unchanged
  </verification>

<success_criteria>

- Phase 14 requirement `REF-01` is satisfied by canonical `template.grid()` availability.
- Phase 14 requirement `REF-02` remains true for engine caller paths after cutover.
- Legacy offset-template entrypoints are no longer the canonical path and fail fast when misused.
  </success_criteria>

<output>
Create `.planning/phases/14-canonical-gridview-api-adoption/14-02-SUMMARY.md` with migration details, parity evidence, and any compatibility notes.
</output>
