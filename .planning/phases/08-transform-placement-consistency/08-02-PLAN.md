---
phase: 08-transform-placement-consistency
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/server/src/server.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - Preview and queue handlers consume identical transform payloads and return taxonomy-consistent legality reasons for transformed and non-transformed placements.
    - If a locally valid preview becomes invalid before queue resolution, server rejection is authoritative and a refreshed preview legality payload is emitted immediately.
    - Integration tests prove transformed preview legality, queue outcomes, and applied simulation footprints stay consistent under multiplayer runtime behavior.
  artifacts:
    - path: apps/server/src/server.ts
      provides: Transform-aware preview/queue runtime handling and authoritative reconciliation behavior
      contains: payload parsing, reason passthrough, and immediate preview refresh after rejection
    - path: tests/integration/server/server.test.ts
      provides: End-to-end transform placement parity assertions
      contains: rotate/mirror preview cases, map-size rejection, and queue/apply parity checks
    - path: tests/integration/server/quality-gate-loop.test.ts
      provides: Deterministic transform-ready integration fixture helpers
      contains: transform-aware placement candidate handling with stable retries
  key_links:
    - from: apps/server/src/server.ts
      to: packages/rts-engine/rts.ts
      via: queue probe and queue submit paths
      pattern: runQueueBuildProbe|queueBuildEvent
    - from: apps/server/src/server.ts
      to: packages/rts-engine/socket-contract.ts
      via: build preview payload construction
      pattern: BuildPreviewPayload|BuildPreviewRequestPayload
    - from: tests/integration/server/server.test.ts
      to: apps/server/src/server.ts
      via: socket contract assertions for preview/queue parity
      pattern: build:preview|build:queue|build:outcome
---

<objective>
Wire server runtime to use transform-aware preview/queue payloads and enforce authoritative parity behavior under real socket flows.

Purpose: Ensure QUAL-03 holds at runtime boundaries by keeping reason taxonomy, legality, and outcome reconciliation consistent when room state changes between preview and queue.
Output: Server handler updates and integration coverage for transformed placement preview/queue/apply parity.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-transform-placement-consistency/08-CONTEXT.md
@.planning/phases/08-transform-placement-consistency/08-RESEARCH.md
@apps/server/src/server.ts
@tests/integration/server/server.test.ts
@tests/integration/server/quality-gate-loop.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: make build preview and queue handlers transform-aware with one rejection taxonomy</name>
  <files>
    apps/server/src/server.ts
  </files>
  <action>
    Extend `build:preview` and `build:queue` payload parsing to accept transform state from clients and pass it through to engine validation. Keep payload guards strict at socket boundaries and preserve existing authoritative gate checks (`not-in-room`, lifecycle, defeated). Emit preview payloads with transformed footprint, illegal-cell details, bounding box metadata, and consistent reason codes from engine outputs. On queue rejection after a recent preview, emit authoritative `room:error` reason and immediately re-probe+emit refreshed preview legality for the same anchor+transform so client state reconciles without manual reselect.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Server runtime accepts transform payloads, preserves reason taxonomy parity across preview/queue, and supports immediate authoritative legality refresh after rejection.</done>
</task>

<task type="auto">
  <name>task 2: lock transformed placement parity in integration suites</name>
  <files>
    tests/integration/server/server.test.ts
    tests/integration/server/quality-gate-loop.test.ts
  </files>
  <action>
    Add integration scenarios that assert rotate and mirror requests (including combined order-sensitive transforms) keep preview legality, queue acceptance/rejection, and `build:outcome` simulation results consistent. Include torus edge-crossing acceptance cases and explicit `template-exceeds-map-size` rejection cases. Add a stale-state reconciliation assertion where preview reports valid but queue is rejected after intervening authoritative state change; verify the follow-up preview message uses the same reason taxonomy and updated legality payload.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts tests/integration/server/quality-gate-loop.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Integration coverage demonstrates runtime QUAL-03 parity for transformed placement and authoritative reconciliation behavior.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/server/server.test.ts tests/integration/server/quality-gate-loop.test.ts` passes.
- `npm run test:integration:serial` passes.
</verification>

<success_criteria>

- QUAL-03 is true at runtime boundary: preview legality, queue result, and applied outcome remain consistent for transformed and non-transformed placements.
- Stale local previews are corrected by authoritative queue rejection plus immediate legality refresh.
- Integration fixtures remain deterministic with transform-aware payloads.
  </success_criteria>

<output>
After completion, create `.planning/phases/08-transform-placement-consistency/08-02-SUMMARY.md`
</output>
