---
phase: 25-training-tui-dashboard
plan: 03
type: execute
wave: 3
depends_on: ['25-01', '25-02']
files_modified:
  - packages/bot-harness/training/tui/plain-logger.ts
  - packages/bot-harness/training/tui/index.ts
  - packages/bot-harness/training/tui/plain-logger.test.ts
  - packages/bot-harness/training/index.ts
  - bin/train.ts
autonomous: false
requirements: [TUI-03, TUI-04]

must_haves:
  truths:
    - 'bin/train.ts renders TUI dashboard when stdout is a TTY and --no-tui is not set'
    - 'bin/train.ts falls back to plain log lines when stdout is not a TTY or --no-tui is set'
    - 'Plain logger uses existing formatLiveMetrics for log output'
    - 'TUI dashboard is importable from #bot-harness barrel'
    - 'Training completes successfully with both TUI and plain modes'
  artifacts:
    - path: 'packages/bot-harness/training/tui/plain-logger.ts'
      provides: 'Non-TTY fallback logger using formatLiveMetrics'
      exports: ['attachPlainLogger']
    - path: 'packages/bot-harness/training/tui/index.ts'
      provides: 'TUI public API barrel'
      exports: ['Dashboard', 'createProgressHandler', 'attachPlainLogger']
    - path: 'bin/train.ts'
      provides: 'Updated CLI with TUI/plain mode gate'
      contains: 'isTTY'
  key_links:
    - from: 'bin/train.ts'
      to: 'packages/bot-harness/training/tui/dashboard.tsx'
      via: 'dynamic import for TUI rendering'
      pattern: "renderDashboard\\|Dashboard"
    - from: 'bin/train.ts'
      to: 'packages/bot-harness/training/tui/plain-logger.ts'
      via: 'import for non-TTY fallback'
      pattern: 'attachPlainLogger'
    - from: 'packages/bot-harness/training/tui/plain-logger.ts'
      to: 'packages/bot-harness/training/training-logger.ts'
      via: 'formatLiveMetrics call'
      pattern: 'formatLiveMetrics'
    - from: 'packages/bot-harness/training/tui/index.ts'
      to: 'packages/bot-harness/training/index.ts'
      via: 'barrel re-export'
      pattern: 'tui/index'
---

<objective>
Create the plain logger fallback, wire TUI and plain modes into bin/train.ts, and create barrel exports so the TUI is importable from #bot-harness.

Purpose: Complete the integration loop -- the coordinator produces data, the TUI or plain logger consumes it, and the user controls mode via TTY detection and --no-tui flag. This is the final plan that makes the TUI usable.

Output: bin/train.ts with conditional TUI/plain rendering, plain-logger fallback, barrel exports, human verification of TUI rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-training-tui-dashboard/25-CONTEXT.md
@.planning/phases/25-training-tui-dashboard/25-RESEARCH.md
@.planning/phases/25-training-tui-dashboard/25-01-SUMMARY.md
@.planning/phases/25-training-tui-dashboard/25-02-SUMMARY.md

@packages/bot-harness/training/tui/types.ts
@packages/bot-harness/training/tui/dashboard.tsx
@packages/bot-harness/training/tui/plain-logger.ts
@packages/bot-harness/training/training-coordinator.ts
@packages/bot-harness/training/training-logger.ts
@packages/bot-harness/training/training-config.ts
@packages/bot-harness/training/index.ts
@packages/bot-harness/index.ts
@bin/train.ts

<interfaces>
<!-- Key exports this plan consumes -->

From packages/bot-harness/training/tui/dashboard.tsx (Plan 02):

```typescript
export const Dashboard: React.FC<{
  onPause: () => void;
  onStop: () => void;
  isPaused: () => boolean;
}>;

export function createProgressHandler(
  setState: React.Dispatch<React.SetStateAction<DashboardState>>,
): TrainingProgressCallback;
```

From packages/bot-harness/training/training-coordinator.ts (Plan 01):

```typescript
export class TrainingCoordinator {
  public onProgress: TrainingProgressCallback | null;
  public togglePause(): void;
  public isPaused(): boolean;
  public requestStop(): void;
  public async init(): Promise<void>;
  public async run(): Promise<void>;
  public async cleanup(): Promise<void>;
  public getWinRate(): number;
  public getEpisodeCounter(): number;
  public getRunId(): string;
  public getLogger(): TrainingLogger | null;
}
```

From packages/bot-harness/training/training-logger.ts:

```typescript
export class TrainingLogger {
  public formatLiveMetrics(
    entry: TrainingLogEntry,
    totalEpisodes: number,
    startTime: number,
  ): string;
}
```

From packages/bot-harness/training/training-config.ts (Plan 01):

```typescript
export interface TrainingConfig {
  // ... existing fields
  noTui: boolean;
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plain logger, barrel exports, and bin/train.ts wiring</name>
  <files>
    packages/bot-harness/training/tui/plain-logger.ts,
    packages/bot-harness/training/tui/plain-logger.test.ts,
    packages/bot-harness/training/tui/index.ts,
    packages/bot-harness/training/index.ts,
    bin/train.ts
  </files>
  <read_first>
    - bin/train.ts (current CLI entry point)
    - packages/bot-harness/training/training-logger.ts (formatLiveMetrics signature)
    - packages/bot-harness/training/training-coordinator.ts (onProgress, togglePause, requestStop)
    - packages/bot-harness/training/tui/types.ts (TrainingProgressCallback, TrainingProgressData)
    - packages/bot-harness/training/tui/dashboard.tsx (Dashboard component, createProgressHandler)
    - packages/bot-harness/training/index.ts (current barrel)
    - packages/bot-harness/index.ts (top-level barrel)
  </read_first>
  <action>
    1. Create `packages/bot-harness/training/tui/plain-logger.ts` (D-06 non-TTY fallback):
       - Export `attachPlainLogger(coordinator: TrainingCoordinator, totalEpisodes: number): void`.
       - Sets `coordinator.onProgress` to a callback that calls `coordinator.getLogger()!.formatLiveMetrics(data.entry, totalEpisodes, data.startTime)` and writes the result to `console.log`.
       - This is the fallback for non-TTY / `--no-tui` mode using the existing `formatLiveMetrics()` method per Pattern 5 in RESEARCH.md.

    2. Create `packages/bot-harness/training/tui/plain-logger.test.ts` (TUI-03):
       - Create a minimal mock coordinator with `onProgress` property, `getLogger()` returning a mock logger with `formatLiveMetrics`.
       - Test that `attachPlainLogger` sets the `onProgress` callback.
       - Test that when the callback fires with mock TrainingProgressData, `formatLiveMetrics` is called and output appears (spy on console.log).

    3. Create `packages/bot-harness/training/tui/index.ts` (barrel):
       - Re-export: `export { Dashboard, createProgressHandler } from './dashboard.js';`
       - Re-export: `export { attachPlainLogger } from './plain-logger.js';`
       - Re-export: `export type { TrainingProgressData, TrainingProgressCallback, DashboardState } from './types.js';`
       - Re-export: `export { HelpOverlay } from './help-overlay.js';`

    4. Update `packages/bot-harness/training/index.ts`:
       - Add: `export * from './tui/index.js';`
       - This makes TUI exports available via `#bot-harness`.

    5. Rewrite `bin/train.ts` to gate TUI vs plain output:
       - Keep the existing `parseTrainingArgs()` call.
       - Keep the coordinator construction.
       - After `coordinator.init()`, determine rendering mode:
         ```typescript
         const useTui = process.stdout.isTTY === true && !config.noTui;
         ```
       - If `useTui`:
         - Dynamic import: `const { render } = await import('ink');`
         - Dynamic import: `const React = await import('react');`
         - Dynamic import dashboard: `const { Dashboard, createProgressHandler } = await import('#bot-harness');`
         - Create the Ink `render()` call with the `<Dashboard>` component, passing `onPause: () => coordinator.togglePause()`, `onStop: () => coordinator.requestStop()`, `isPaused: () => coordinator.isPaused()`.
         - Wire `coordinator.onProgress` using `createProgressHandler` or by passing the coordinator to Dashboard (either approach works -- Dashboard's internal state management handles it).
         - Use `{ patchConsole: true, exitOnCtrlC: false }` in render options (Ctrl+C handled by existing SIGINT handler).
         - Start training: `await coordinator.run()`.
         - After run completes, call `inkInstance.unmount()` to cleanly exit Ink.
       - If NOT `useTui`:
         - Print startup banner to console (move existing banner here).
         - Import and call `attachPlainLogger(coordinator, config.totalEpisodes)`.
         - Start training: `await coordinator.run()`.
       - Keep existing SIGINT/SIGTERM handlers but update them:
         - In TUI mode: also unmount Ink before cleanup.
         - In both modes: call `coordinator.requestStop()` first, then cleanup.
       - After training completes, print completion summary (episodes, win rate, duration, run dir). In TUI mode, print after Ink unmount.
       - Per D-12: pause stops episode collection via coordinator.togglePause(). TUI keeps rendering because Ink's React loop is independent of the training loop.
       - Per Pitfall 3: the coordinator communicates via the onProgress callback, not console.log. Remove any remaining console.log from the coordinator's runtime path in bin/train.ts.
       - Per Pitfall 5: worker threads use postMessage only, no console.log.

  </action>
  <verify>
    <automated>npx vitest run packages/bot-harness/training/tui/plain-logger.test.ts -x && npx tsc -p tsconfig.json --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'attachPlainLogger' packages/bot-harness/training/tui/plain-logger.ts
    - grep -q 'formatLiveMetrics' packages/bot-harness/training/tui/plain-logger.ts
    - grep -q 'Dashboard' packages/bot-harness/training/tui/index.ts
    - grep -q 'attachPlainLogger' packages/bot-harness/training/tui/index.ts
    - grep -q 'tui/index' packages/bot-harness/training/index.ts
    - grep -q 'isTTY' bin/train.ts
    - grep -q 'noTui' bin/train.ts
    - grep -q 'attachPlainLogger' bin/train.ts
    - grep -q 'Dashboard' bin/train.ts
    - grep -q 'togglePause' bin/train.ts
    - grep -q 'requestStop' bin/train.ts
  </acceptance_criteria>
  <done>Plain logger fallback works. TUI barrel exports are available from #bot-harness. bin/train.ts conditionally renders Ink TUI when TTY and not --no-tui, or plain logger otherwise. Lint and type-check pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify TUI dashboard renders correctly</name>
  <files>bin/train.ts</files>
  <action>
    Human visually verifies the TUI dashboard by running training in both TUI and plain modes.
  </action>
  <verify>
    <automated>npx tsx bin/train.ts --episodes 10 --batch-episodes 2 --workers 1 --no-tui</automated>
  </verify>
  <what-built>
    Complete TUI dashboard for PPO training: two-column layout with progress bar, ASCII charts, colored metrics, opponent pool status, keyboard controls, and plain-mode fallback.
  </what-built>
  <how-to-verify>
    1. Run a short training session with TUI enabled:
       ```
       npx tsx bin/train.ts --episodes 20 --batch-episodes 4 --workers 1
       ```
       Verify:
       - Dashboard renders with two columns (if terminal >= 100 chars wide)
       - Progress bar updates as episodes complete
       - ASCII charts appear for reward and loss trends
       - Metrics table shows win rate, entropy, KL, ETA
       - Opponent pool status section visible

    2. Test keyboard controls:
       - Press Space: training should pause (dashboard shows PAUSED)
       - Press Space again: training resumes
       - Press h: help overlay appears
       - Press h again: help overlay disappears
       - Press q: training stops gracefully

    3. Test plain mode fallback:
       ```
       npx tsx bin/train.ts --episodes 10 --batch-episodes 2 --workers 1 --no-tui
       ```
       Verify: plain log lines print one per episode (no TUI rendering).

    4. Test piped output:
       ```
       npx tsx bin/train.ts --episodes 10 --batch-episodes 2 --workers 1 | head -20
       ```
       Verify: plain log lines (no ANSI escape sequences for TUI).

  </how-to-verify>
  <done>User confirms TUI renders correctly in both two-column and plain modes. Keyboard controls work. Piped output degrades gracefully.</done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run packages/bot-harness/training/tui -x` -- all TUI tests pass
- `npm run test:unit` -- all package unit tests pass (no regressions)
- `npx tsc -p tsconfig.json --noEmit` -- clean
- `npx tsc -p tsconfig.server.json --noEmit` -- clean
- `npm run lint` -- clean
- `npx tsx bin/train.ts --episodes 10 --no-tui --workers 1 --batch-episodes 2` -- completes with plain log output
</verification>

<success_criteria>

- bin/train.ts detects TTY and --no-tui flag to choose rendering mode
- TUI mode renders Ink dashboard with live-updating metrics
- Plain mode prints log lines using formatLiveMetrics
- All unit tests pass including new TUI tests
- Lint and type-check pass
- User visually confirms TUI renders correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/25-training-tui-dashboard/25-03-SUMMARY.md`
</output>
