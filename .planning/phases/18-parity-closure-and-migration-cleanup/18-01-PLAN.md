---
phase: 18-parity-closure-and-migration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/template-grid-authoritative.test.ts
  - packages/rts-engine/rts.test.ts
autonomous: true
requirements:
  - REF-08
  - REF-09
must_haves:
  truths:
    - 'Representative transformed preview, queue, and execute checkpoints remain deterministic after migration-only harness retirement.'
    - 'Authoritative helper tests verify canonical rejection taxonomy and affordability semantics without legacy mirror comparators.'
    - 'Temporary old-vs-new migration markers are removed from unit suites while parity checks remain green.'
  artifacts:
    - path: packages/rts-engine/template-grid-authoritative.test.ts
      provides: canonical authoritative evaluation parity coverage for transformed success and rejection scenarios
      contains: explicit expected outcome assertions replacing temporary legacy mirror harnesses
    - path: packages/rts-engine/rts.test.ts
      provides: deterministic action-boundary parity contract checks for preview, queue, and execute timelines
      contains: representative transformed timeline checkpoints without temporary migration-only comments
  key_links:
    - from: packages/rts-engine/template-grid-authoritative.test.ts
      to: packages/rts-engine/template-grid-authoritative.ts
      via: evaluate/project/apply helper assertions validate authoritative behavior directly
      pattern: evaluateAuthoritativeBuildPlacement|projectAuthoritativeBuildPlacement|applyAuthoritativeBuildProjection
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: preview/queue/tick timeline assertions enforce unchanged runtime parity behavior
      pattern: previewBuildPlacement|queueBuildEvent|tickRoom
---

<objective>
Retire Phase 17 temporary migration parity scaffolding in unit suites while preserving and strengthening deterministic parity contracts for transformed preview, queue, and execute behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-parity-closure-and-migration-cleanup/18-CONTEXT.md
@.planning/phases/18-parity-closure-and-migration-cleanup/18-RESEARCH.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-01-SUMMARY.md
@packages/rts-engine/template-grid-authoritative.test.ts
@packages/rts-engine/rts.test.ts
</context>

<tasks>
  <task type="auto">
    <name>Replace temporary legacy-mirror assertions in authoritative helper tests with canonical expected-outcome checks</name>
    <files>
      - packages/rts-engine/template-grid-authoritative.test.ts
    </files>
    <action>
      Remove temporary migration-only old-vs-new comparator scaffolding and replace it with explicit transformed scenario assertions over canonical outcomes.
      Preserve representative scenario coverage for success, `outside-territory`, `template-exceeds-map-size`, and `insufficient-resources`, including affordability metadata and projection invariants.
      Keep assertions deterministic and readable so future parity drift is diagnosable without reintroducing legacy mirror logic.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-authoritative.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Authoritative helper parity coverage no longer depends on temporary legacy-mirror harness code and still validates representative transformed outcomes.
    </done>
  </task>

  <task type="auto">
    <name>Promote representative action timeline parity checks in `rts.test.ts` from migration guard to permanent contract gate</name>
    <files>
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Remove migration-only marker language and tighten representative transformed timeline assertions so preview, queue acceptance, outcome cadence, affordability metadata, and resource/structure checkpoints remain explicitly validated.
      Keep deterministic rerun equivalence checks that compare full normalized checkpoints across repeated executions.
      Avoid broad unrelated test churn; limit edits to parity closure behavior and test clarity.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"
        - npx vitest run packages/rts-engine/rts.test.ts -t "preserves occupied-site precedence over insufficient resources at execute time"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Representative runtime parity checks remain deterministic and migration-only guard markers are removed from the suite.
    </done>
  </task>

  <task type="auto">
    <name>Prove unit-level parity closure after migration guard retirement</name>
    <files>
      - packages/rts-engine/template-grid-authoritative.test.ts
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Run the unit parity command set after harness retirement and resolve any assertion drift discovered during targeted execution.
      Keep outcomes aligned with existing authoritative contract semantics (rejection taxonomy, affordability fields, and structure/resource checkpoints).
      Record any residual non-phase failures explicitly in the summary rather than widening scope.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-authoritative.test.ts
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Unit parity closure is validated with temporary migration-only assertions removed and targeted parity tests passing.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/template-grid-authoritative.test.ts`
- `npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"`
- `npx vitest run packages/rts-engine/rts.test.ts -t "preserves occupied-site precedence over insufficient resources at execute time"`

Confirm:

- unit parity contracts for transformed authoritative evaluation remain deterministic and explicit
- migration-only old-vs-new guard scaffolding is absent from unit suites
- representative preview/queue/execute parity checkpoints remain stable after cleanup
  </verification>

<success_criteria>

- Requirements `REF-08` and `REF-09` are advanced by replacing temporary migration guards with permanent unit parity contracts.
- Temporary migration assertion markers are removed from `template-grid-authoritative.test.ts` and `rts.test.ts`.
- Targeted parity unit tests continue to pass without behavior drift.
  </success_criteria>

<output>
Create `.planning/phases/18-parity-closure-and-migration-cleanup/18-01-SUMMARY.md` with details of migration guard retirement, updated parity assertions, and command evidence.
</output>
