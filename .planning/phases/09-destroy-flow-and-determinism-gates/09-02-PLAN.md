---
phase: 09-destroy-flow-and-determinism-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/server/src/server.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
  - tests/integration/server/destroy-determinism.test.ts
autonomous: true
requirements:
  - STRUCT-02
  - QUAL-04
must_haves:
  truths:
    - 'Active players can enqueue destroy actions through runtime handlers and receive deterministic queued or rejected results.'
    - 'Wrong-owner, invalid-target, and invalid-lifecycle-state destroy attempts are rejected with stable reason codes and no side effects.'
    - 'Both connected clients observe one terminal destroy outcome per accepted request with matching event and tick metadata.'
    - 'Reconnect scenarios converge to the same authoritative structure and build-eligibility state without client divergence.'
  artifacts:
    - path: 'apps/server/src/server.ts'
      provides: 'Socket runtime destroy queue handlers and deterministic reason passthrough'
      contains: 'destroy payload parsing, gameplay gates, queued ack/outcome emission, reconnect-safe state rebroadcast compatibility'
    - path: 'tests/integration/server/server.test.ts'
      provides: 'Two-client runtime destroy acceptance and rejection coverage'
      contains: 'owner success path, wrong-owner reject, invalid-target reject, invalid-lifecycle-state reject, terminal outcome parity'
    - path: 'tests/integration/server/destroy-determinism.test.ts'
      provides: 'Focused reconnect determinism coverage for pending and resolved destroy actions'
      contains: 'disconnect/reconnect convergence assertions across state and destroy outcomes'
    - path: 'tests/integration/server/quality-gate-loop.test.ts'
      provides: 'QUAL-04 gate scenario for build plus destroy determinism'
      contains: 'phase-level deterministic loop with reconnect-safe state parity checks'
  key_links:
    - from: 'apps/server/src/server.ts'
      to: 'packages/rts-engine/rts.ts'
      via: 'destroy queue and tick outcome plumbing'
      pattern: 'queueDestroyEvent|destroyOutcomes|tickRoom'
    - from: 'apps/server/src/server.ts'
      to: 'packages/rts-engine/socket-contract.ts'
      via: 'destroy socket event payload typing'
      pattern: 'destroy:queue|destroy:queued|destroy:outcome'
    - from: 'tests/integration/server/destroy-determinism.test.ts'
      to: 'apps/server/src/server.ts'
      via: 'runtime contract assertions across reconnect boundaries'
      pattern: 'room:joined|state|destroy:outcome'
---

<objective>
Wire destroy actions through Socket.IO runtime and lock two-client plus reconnect determinism behavior with integration coverage.

Purpose: Ensure STRUCT-02 and QUAL-04 hold at runtime boundaries, not only in package-level unit logic.
Output: Server destroy event handlers and integration suites that prove deterministic outcomes and state convergence.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-CONTEXT.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-RESEARCH.md
@apps/server/src/server.ts
@tests/integration/server/server.test.ts
@tests/integration/server/quality-gate-loop.test.ts
@tests/integration/server/lobby-reconnect.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: implement destroy queue socket handlers with deterministic runtime gates</name>
  <files>
    apps/server/src/server.ts
  </files>
  <action>
    Add destroy runtime handlers that parse typed payloads, enforce existing gameplay mutation gates, and route accepted requests into engine destroy queue APIs. Emit deterministic queued acknowledgements and room-scoped destroy outcomes with stable reason taxonomy passthrough. Keep runtime behavior idempotent for duplicate same-target requests and avoid opponent-only custom destroy notifications; opponents infer state changes through authoritative state and shared outcomes. Ensure reconnecting sessions can resubscribe and converge via existing authoritative state emission loop.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Server runtime exposes destroy queue and terminal outcome behavior with deterministic reason handling and reconnect-safe state broadcasts.</done>
</task>

<task type="auto">
  <name>task 2: add two-client and reconnect integration determinism gates for destroy flow</name>
  <files>
    tests/integration/server/server.test.ts
    tests/integration/server/quality-gate-loop.test.ts
    tests/integration/server/destroy-determinism.test.ts
  </files>
  <action>
    Extend integration coverage to assert accepted owned destroy actions, deterministic rejection reasons (`wrong-owner`, `invalid-target`, `invalid-lifecycle-state`), and exactly one terminal destroy outcome per accepted event across both clients. Add reconnect scenarios where pending or recently applied destroy actions still converge to identical authoritative state after rejoin. Extend the quality-gate loop with a QUAL-04-tagged build-plus-destroy scenario that verifies deterministic parity across both clients and reconnect checkpoints.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts tests/integration/server/destroy-determinism.test.ts tests/integration/server/quality-gate-loop.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Integration suites prove runtime destroy flow is deterministic for two-client and reconnect scenarios and closes QUAL-04 coverage gaps.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/integration/server/server.test.ts tests/integration/server/destroy-determinism.test.ts tests/integration/server/quality-gate-loop.test.ts` passes.
- `npm run test:integration:serial` passes.
</verification>

<success_criteria>

- STRUCT-02 is true at runtime: destroy queue events are accepted only for legal owned targets and emit authoritative outcomes.
- QUAL-04 runtime gate is true: two-client and reconnect integration flows converge on identical structure and build-eligibility state.
- Destroy rejection taxonomy remains deterministic and stable at socket contract boundaries.
  </success_criteria>

<output>
After completion, create `.planning/phases/09-destroy-flow-and-determinism-gates/09-02-SUMMARY.md`
</output>
