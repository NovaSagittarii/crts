---
phase: 09-destroy-flow-and-determinism-gates
plan: 01
type: execute
wave: 1
depends_on:
  - 08-03
files_modified:
  - packages/rts-engine/rts.ts
  - packages/rts-engine/socket-contract.ts
  - packages/rts-engine/rts.test.ts
autonomous: true
requirements:
  - STRUCT-02
  - QUAL-04
must_haves:
  truths:
    - 'Players can queue destroy requests for owned structures and receive one deterministic authoritative outcome per accepted event.'
    - 'Destroy attempts targeting wrong-owner, invalid-target, or invalid-lifecycle-state paths are rejected with stable machine reasons.'
    - 'Accepted destroy actions remove build-zone contribution and remain reconnect-safe through authoritative state projection.'
    - 'Duplicate same-target requests are idempotent while a different target may still be queued during pending state.'
  artifacts:
    - path: 'packages/rts-engine/rts.ts'
      provides: 'Deterministic destroy queue validation, execution, and state projection'
      contains: 'destroy queue payload and outcome types, lifecycle reason taxonomy, same-target idempotency, projected structure fields'
    - path: 'packages/rts-engine/socket-contract.ts'
      provides: 'Shared destroy payload contracts and reason enums'
      contains: 'destroy queue request, queued ack, destroy outcome, rejection reason transport typing'
    - path: 'packages/rts-engine/rts.test.ts'
      provides: 'Unit determinism coverage for destroy acceptance and rejection'
      contains: 'owner checks, stale lifecycle rejects, idempotent duplicate behavior, deterministic tick replay assertions'
  key_links:
    - from: 'packages/rts-engine/rts.ts'
      to: 'packages/rts-engine/socket-contract.ts'
      via: 'destroy queue and outcome type exports'
      pattern: 'DestroyQueuePayload|DestroyOutcome|DestroyRejectionReason'
    - from: 'packages/rts-engine/rts.ts'
      to: 'packages/rts-engine/rts.test.ts'
      via: 'destroy queue and tick resolution assertions'
      pattern: 'queueDestroyEvent|invalid-lifecycle-state|wrong-owner'
---

<objective>
Create the authoritative destroy pipeline in shared engine logic so destroy actions are deterministic, reconnect-safe, and ready for runtime wiring.

Purpose: STRUCT-02 and QUAL-04 both require one canonical source of truth for destroy validation, queueing, execution ordering, and rejection taxonomy.
Output: Engine destroy queue support, shared contract types, and deterministic unit coverage for destroy and build-zone effects.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-CONTEXT.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-RESEARCH.md
@packages/rts-engine/rts.ts
@packages/rts-engine/socket-contract.ts
@packages/rts-engine/rts.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add deterministic destroy queue model and rejection taxonomy in engine contracts</name>
  <files>
    packages/rts-engine/rts.ts
    packages/rts-engine/socket-contract.ts
  </files>
  <action>
    Introduce destroy queue payload and outcome types in shared engine and socket contract surfaces. Add authoritative validation that enforces owned-structure targeting, deterministic invalid-target handling, and lifecycle-state rejection for already-destroyed or unavailable structures. Keep same-target duplicate requests idempotent (no duplicate queued event), while allowing pending retarget to a different structure. Represent base-destroy confirmation as a structure-level property in authoritative projection so UI can gate confirmation without hardcoding template IDs.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Destroy queue APIs and shared typing exist with deterministic reason codes, idempotent duplicate handling, and confirmation metadata support.</done>
</task>

<task type="auto">
  <name>task 2: execute destroy events in deterministic tick flow and lock unit replay coverage</name>
  <files>
    packages/rts-engine/rts.ts
    packages/rts-engine/rts.test.ts
  </files>
  <action>
    Integrate due destroy event processing into tick execution with explicit deterministic ordering relative to other queued mutations. On accepted destroy, deactivate the structure, zero its build-radius contribution, and preserve debris semantics without rewriting Conway cells. Extend room state projection so reconnecting clients can recover current structure and pending destroy status from authoritative snapshots. Add unit tests for accepted owner destroy, wrong-owner reject, invalid-target reject, invalid-lifecycle-state reject, duplicate idempotency, and build-zone shrink after destroy; include one equal-run replay assertion proving identical outcomes across repeated simulation seeds.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/match-lifecycle.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Destroy queue execution is deterministic, rejection reasons are stable, and state projection supports reconnect-safe parity checks.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/match-lifecycle.test.ts` passes.
- `npm run test:unit` passes.
</verification>

<success_criteria>

- STRUCT-02 engine foundation is true: owned destroy requests have one authoritative outcome path and deterministic rejection taxonomy.
- QUAL-04 engine gate is true: equal-run destroy and build ordering yields stable outcomes and projected state.
- Accepted destroys immediately stop structure build-zone contribution and project reconnect-safe structure metadata.
  </success_criteria>

<output>
After completion, create `.planning/phases/09-destroy-flow-and-determinism-gates/09-01-SUMMARY.md`
</output>
