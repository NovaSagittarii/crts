---
phase: 06-base-geometry-and-integrity-core
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/base-geometry.ts
  - packages/rts-engine/base-geometry.test.ts
  - packages/rts-engine/spawn.ts
  - packages/rts-engine/spawn.test.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - packages/rts-engine/index.ts
autonomous: true
requirements:
  - BASE-01
must_haves:
  truths:
    - New matches seed every team with the same canonical 5x5 footprint containing exactly 16 base cells.
    - Territory validation and counting use geometry-derived base center math for the 5x5 footprint (no hardcoded +1 assumptions).
    - Spawn overlap and minimum wrapped-distance checks use canonical base dimensions and stay deterministic.
  artifacts:
    - path: packages/rts-engine/base-geometry.ts
      provides: Canonical base geometry helpers for dimensions, occupied cells, and center derivation.
    - path: packages/rts-engine/rts.ts
      provides: Base seeding, base center territory logic, and room initialization wired to shared geometry helpers.
    - path: packages/rts-engine/spawn.ts
      provides: Torus spawn layout defaults and overlap checks that consume canonical base dimensions.
    - path: packages/rts-engine/rts.test.ts
      provides: Requirement-tagged base footprint and territory behavior tests for BASE-01.
    - path: packages/rts-engine/spawn.test.ts
      provides: Spawn spacing and overlap tests aligned with 5x5 footprint assumptions.
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/base-geometry.ts
      via: Base seeding and territory center/counting call shared helpers instead of inline footprint math.
      pattern: BASE_5X5|getBaseCenter|isCanonicalBaseCell
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/spawn.ts
      via: addPlayerToRoom passes canonical base width/height and min distance into spawn layout.
      pattern: createTorusSpawnLayout|baseWidth|baseHeight|minWrappedDistance
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: Tests assert canonical 5x5 cell occupancy and territory acceptance/rejection against the same helpers.
      pattern: BASE-01|baseTopLeft|outside-territory
---

<objective>
Establish a single canonical 5x5 base geometry contract and wire spawn plus territory math to that contract so all base-related rules use identical deterministic coordinates.

Purpose: Eliminate divergent hardcoded footprint math before integrity and defeat logic expands in later plans.
Output: Shared base-geometry helpers, 5x5-aware engine wiring, and updated deterministic unit tests for BASE-01.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-base-geometry-and-integrity-core/06-CONTEXT.md
@packages/rts-engine/rts.ts
@packages/rts-engine/spawn.ts
@packages/rts-engine/rts.test.ts
@packages/rts-engine/spawn.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add canonical 5x5 base geometry helpers and export them for engine use</name>
  <files>packages/rts-engine/base-geometry.ts, packages/rts-engine/base-geometry.test.ts, packages/rts-engine/index.ts</files>
  <action>Create a dedicated base-geometry module defining canonical 5x5 constants (width/height, center offset, and occupied-cell membership for the four corner 2x2 blocks). Include helpers for iterating occupied base cells and deriving canonical base center from `team.baseTopLeft`. Keep this module deterministic/pure and export it from `packages/rts-engine/index.ts`. Do not duplicate the same coordinate logic inline in multiple engine files, because phase-6 decisions require one shared geometry contract.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/base-geometry.test.ts</automated>
  </verify>
  <done>`packages/rts-engine/base-geometry.ts` is the single source of truth for canonical 5x5 footprint math and its unit tests pass.</done>
</task>

<task type="auto">
  <name>task 2: rewire base seeding and territory checks in rts engine to canonical geometry</name>
  <files>packages/rts-engine/rts.ts, packages/rts-engine/rts.test.ts</files>
  <action>Update room initialization and base logic to consume shared base-geometry helpers: seed only canonical occupied cells (16 total), derive base center as `(baseTopLeft.x + 2, baseTopLeft.y + 2)`, and remove legacy `+1` assumptions from territory placement and territory counting checks. Ensure core/base-related template dimensions align with the 5x5 footprint and that requirement-tagged unit tests assert the locked shape (`##.## / ##.## / ..... / ##.## / ##.##`) and breach-pressure consistency expectations for BASE-01.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
  </verify>
  <done>Base footprint, center math, and territory legality in `rts.ts` all use canonical helpers; BASE-01 unit assertions pass.</done>
</task>

<task type="auto">
  <name>task 3: align spawn defaults and overlap tests with canonical 5x5 base dimensions</name>
  <files>packages/rts-engine/spawn.ts, packages/rts-engine/spawn.test.ts</files>
  <action>Update torus spawn layout defaults and overlap/min-distance calculations to use canonical base dimensions from shared geometry (including `baseWidth + 1` baseline distance for this phase). Keep deterministic orientation behavior unchanged. Revise spawn tests so center/overlap calculations are validated against 5x5 assumptions and still fail deterministically on overlap regressions.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/spawn.test.ts</automated>
  </verify>
  <done>Spawn layout logic and tests are 5x5-aware, deterministic, and consistent with canonical base geometry.</done>
</task>

</tasks>

<verification>
Run `npx vitest run packages/rts-engine/base-geometry.test.ts packages/rts-engine/spawn.test.ts packages/rts-engine/rts.test.ts` and confirm all BASE-01 geometry/territory/spawn assertions pass under the canonical 5x5 contract.
</verification>

<success_criteria>

- Canonical 5x5 base geometry exists in one shared helper module and is exported for engine consumers.
- Match initialization, territory checks, and spawn overlap math no longer use legacy 2x2/`+1` assumptions.
- Unit tests deterministically prove 16-cell base occupancy and 5x5-consistent territory/spawn behavior.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-base-geometry-and-integrity-core/06-01-SUMMARY.md`
</output>
