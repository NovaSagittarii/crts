---
phase: 06-base-geometry-and-integrity-core
plan: '02'
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/match-lifecycle.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
autonomous: true
requirements:
  - STRUCT-01
  - BASE-01
must_haves:
  truths:
    - Every player-owned structure runs deterministic integrity evaluation every tick during active simulation.
    - Failed integrity checks cost exactly 1 HP and restore structure cells in the same integrity phase when HP remains above zero.
    - Core HP reaching zero marks team defeat once, drains pending queue outcomes with `team-defeated`, and preserves canonical winner ranking.
    - `baseIntact` is true only when core HP is above zero and canonical 5x5 base integrity checks pass.
  artifacts:
    - path: packages/rts-engine/rts.ts
      provides: Authoritative template-wide integrity resolution phase with fixed cadence and deterministic ordering.
    - path: packages/rts-engine/rts.test.ts
      provides: STRUCT-01/BASE-01 unit coverage for HP loss, same-phase repair, defeat trigger, and queue drain semantics.
    - path: tests/integration/server/match-lifecycle.test.ts
      provides: Integration validation that match finish outcomes and ranking stay deterministic under 5x5 breach pressure.
    - path: tests/integration/server/server.test.ts
      provides: Integration validation for authoritative state/output behavior after integrity-driven defeats.
    - path: tests/integration/server/quality-gate-loop.test.ts
      provides: Defeated-team rejection and deterministic loop coverage after integrity resolution changes.
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/rts.ts
      via: `tickRoom` invokes one integrity resolution stage that is the only place HP/active/restoration writes occur.
      pattern: tickRoom|resolveStructureIntegrity|hp|core-damaged|core-destroyed
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/rts.test.ts
      via: Unit tests assert fixed cadence (`K=1`), HP decrement of 1, same-phase repair, and deterministic defeat queue drain.
      pattern: STRUCT-01|team-defeated|buildOutcomes|baseIntact
    - from: apps/server/src/server.ts
      to: tests/integration/server/match-lifecycle.test.ts
      via: Integration tests confirm authoritative room events/state payloads still match deterministic ranking and defeated lockout contracts.
      pattern: room:match-finished|build:outcome|defeated|baseIntact
---

<objective>
Generalize integrity damage/repair to all player-owned structures with deterministic cadence/order while preserving canonical defeat, queue-drain, and outcome behavior under the new 5x5 base model.

Purpose: Deliver STRUCT-01 and finalize phase-6 deterministic breach semantics so downstream build-zone and transform phases can rely on stable base/integrity outcomes.
Output: Template-wide integrity resolution in engine plus requirement-tagged unit/integration coverage for deterministic behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-base-geometry-and-integrity-core/06-CONTEXT.md
@.planning/phases/02-match-lifecycle-breach-outcomes/02-01-SUMMARY.md
@.planning/phases/03-deterministic-build-queue-validation/03-01-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/rts.test.ts
@tests/integration/server/server.test.ts
@tests/integration/server/match-lifecycle.test.ts
@tests/integration/server/quality-gate-loop.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: implement template-wide integrity resolution loop with locked cadence and hp rules</name>
  <files>packages/rts-engine/rts.ts</files>
  <action>Create a single authoritative integrity phase in `tickRoom` that runs every tick (`K=1`) and processes all player-owned structures in deterministic order (team ID ascending, then stable structure ordering). On failed integrity: subtract exactly 1 HP; if resulting HP remains above zero, immediately restore template cells in the same integrity phase; if HP reaches zero, structure remains destroyed. Apply this to core and non-core structures; initialize non-core structure HP at 2 and keep core HP at 3 per locked phase-6 decisions. Keep queue/preview paths read-only and avoid HP/integrity writes outside this integrity phase.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
  </verify>
  <done>All structures follow the same deterministic K=1 integrity HP/restore loop, with writes centralized to one authoritative tick phase.</done>
</task>

<task type="auto">
  <name>task 2: finalize defeat and baseIntact semantics for 5x5 integrity checks</name>
  <files>packages/rts-engine/rts.ts, packages/rts-engine/rts.test.ts</files>
  <action>Update `baseIntact` to require both core HP > 0 and canonical 5x5 base integrity success (shared geometry helper), not just legacy core-template checks. Preserve existing canonical ranking comparator fields while ensuring core destruction still marks defeat once and drains pending queued builds deterministically with `team-defeated` before `match-finished` drains. Add or update requirement-tagged unit tests covering same-phase repair, core destruction trigger, stable outcome ranking, and deterministic drain ordering.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
  </verify>
  <done>Unit tests prove 5x5-aware `baseIntact`, deterministic defeat transitions, and stable queue-drain outcome semantics.</done>
</task>

<task type="auto">
  <name>task 3: extend multiplayer integration checks for integrity-driven outcomes</name>
  <files>tests/integration/server/server.test.ts, tests/integration/server/match-lifecycle.test.ts, tests/integration/server/quality-gate-loop.test.ts</files>
  <action>Add/adjust integration scenarios to assert phase-6 behavior from the socket contract: deterministic match-finished ranking after integrity-driven breaches, authoritative `baseIntact` state transitions, and defeated-player rejection behavior after terminal outcomes. Reuse existing bounded retry/event buffering helpers; do not introduce unbounded sleeps or non-deterministic timing assertions.</action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts</automated>
  </verify>
  <done>Integration suites pass with explicit assertions for deterministic integrity-driven outcomes, defeated lockout, and authoritative payload consistency.</done>
</task>

</tasks>

<verification>
Run `npx vitest run packages/rts-engine/rts.test.ts tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts` and confirm STRUCT-01 + BASE-01 deterministic integrity/defeat behavior passes end-to-end.
</verification>

<success_criteria>

- Structure integrity checks run every tick for all player-owned templates with deterministic ordering and HP rules.
- Core defeat, queue drain reasons, and canonical ranking remain deterministic after integrity expansion.
- `baseIntact` reflects 5x5 integrity + core HP state and is proven in unit and integration suites.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-base-geometry-and-integrity-core/06-02-SUMMARY.md`
</output>
