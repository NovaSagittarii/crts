---
phase: 06-base-geometry-and-integrity-core
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/match-lifecycle.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
autonomous: true
requirements:
  - STRUCT-01
  - BASE-01
must_haves:
  truths:
    - 'Every player-owned structure is integrity-checked on a fixed cadence of K=1 tick in active matches.'
    - 'When a structure integrity check fails, HP decreases by exactly 1, and integrity restores immediately if HP remains above zero.'
    - 'Core reaching 0 HP remains the only defeat trigger, with deterministic queue-drain outcomes (`team-defeated`, `match-finished`).'
    - '`baseIntact` is true only when core HP is positive and canonical 5x5 base-integrity checks pass.'
  artifacts:
    - path: 'packages/rts-engine/rts.ts'
      provides: 'Template-wide deterministic integrity/HP resolver in authoritative tick phase'
      contains: 'team-sorted and structure-stable evaluation order'
    - path: 'packages/rts-engine/rts.test.ts'
      provides: 'Unit coverage for template-wide integrity damage/repair semantics'
      contains: 'default integrity mask behavior and deterministic ordering checks'
    - path: 'tests/integration/server/match-lifecycle.test.ts'
      provides: 'Integration coverage for defeat/outcome stability under new geometry/integrity'
      contains: 'stable match-finished payload and reason assertions'
  key_links:
    - from: 'packages/rts-engine/rts.ts'
      to: 'packages/rts-engine/rts.test.ts'
      via: 'integrity resolver behavior'
      pattern: 'resolve.*Integrity|checkStructureIntegrity|hp'
    - from: 'packages/rts-engine/rts.ts'
      to: 'tests/integration/server/server.test.ts'
      via: 'room payload baseIntact and placement legality behavior'
      pattern: 'baseIntact|build:queue'
    - from: 'packages/rts-engine/rts.ts'
      to: 'tests/integration/server/match-lifecycle.test.ts'
      via: 'core defeat to match-finished outcome pipeline'
      pattern: 'coreHpBeforeResolution|team-defeated|room:match-finished'
---

<objective>
Implement deterministic template-wide integrity damage/repair while preserving existing core-defeat lifecycle guarantees.

Purpose: Deliver STRUCT-01 without regressing canonical queue/outcome semantics and while enforcing BASE-01 payload truth (`baseIntact`).
Output: Generalized integrity resolution in engine plus unit/integration coverage proving deterministic outcomes.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-base-geometry-and-integrity-core/06-CONTEXT.md
@.planning/phases/06-base-geometry-and-integrity-core/06-01-SUMMARY.md
@.planning/phases/02-match-lifecycle-breach-outcomes/02-01-SUMMARY.md
@.planning/phases/03-deterministic-build-queue-validation/03-01-SUMMARY.md
@.planning/phases/03-deterministic-build-queue-validation/03-02-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/rts.test.ts
@tests/integration/server/server.test.ts
@tests/integration/server/match-lifecycle.test.ts
@tests/integration/server/quality-gate-loop.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: generalize integrity resolution across all player-owned templates</name>
  <files>
    packages/rts-engine/rts.ts
  </files>
  <action>
    Replace the core-only restore phase with a deterministic template-wide integrity phase that runs every tick (`K=1`) and mutates state only in the authoritative tick pipeline. Evaluate structures in deterministic order (team id ascending, then stable structure ordering), apply integrity masks from template checks, and when checks are absent default to all template cells. On integrity failure, subtract exactly 1 HP, then immediately restore template cells if HP remains above zero. Preserve locked lifecycle semantics: core at 0 HP is still the only defeat trigger, defeat is marked once, and pending queue outcomes remain deterministically drained with existing reason codes.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Engine enforces template-wide integrity checks and HP repair deterministically with unchanged authoritative defeat and queue-drain behavior.</done>
</task>

<task type="auto">
  <name>task 2: extend unit and integration fixtures for base-intact and deterministic outcomes</name>
  <files>
    packages/rts-engine/rts.test.ts
    tests/integration/server/server.test.ts
    tests/integration/server/match-lifecycle.test.ts
    tests/integration/server/quality-gate-loop.test.ts
  </files>
  <action>
    Add or update requirement-traceable tests for STRUCT-01 and BASE-01: non-core templates with empty `checks` must be integrity-tracked via full-cell masks, HP loss must be exactly 1 per failed integrity cycle, and restore must happen in-phase when HP remains. Update integration helpers that derive candidate placement and breach coordinates so they respect the canonical 5x5 footprint, then keep existing lifecycle assertions intact (`coreHpBeforeResolution` ranking key, `team-defeated` and `match-finished` reasons, restart/base-intact expectations). Do not introduce non-deterministic listener timing; register/buffer listeners before emits.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Server-facing tests prove template-wide integrity and base-intact behavior are deterministic and reconnect-safe under canonical geometry.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/rts.test.ts tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts` passes.
- `npm run test:quality` passes with no new deterministic-order failures.
</verification>

<success_criteria>

- STRUCT-01 is observable in unit/integration tests: all player-owned structures are integrity-checked every tick, each failed check costs exactly 1 HP, and surviving structures restore immediately.
- Core defeat remains the sole defeat trigger and continues to produce deterministic ranking and queue-drain outcomes.
- BASE-01 remains true in runtime payloads: `baseIntact` reflects both core HP and canonical 5x5 integrity status.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-base-geometry-and-integrity-core/06-02-SUMMARY.md`
</output>
