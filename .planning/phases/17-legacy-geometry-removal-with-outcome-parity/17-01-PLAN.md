---
phase: 17-legacy-geometry-removal-with-outcome-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/template-grid-authoritative.ts
  - packages/rts-engine/template-grid-authoritative.test.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/index.ts
  - packages/rts-engine/rts.test.ts
autonomous: true
requirements:
  - REF-06
must_haves:
  truths:
    - 'Authoritative build evaluation uses one canonical transformed-geometry helper path, and duplicate template/offset-template traversal logic is removed from `rts.ts`.'
    - 'Equivalent invalid actions preserve rejection reason taxonomy and produce no resource or structure side effects.'
    - 'Representative transformed success and rejection flows remain deterministic at preview, queue, and execute-time boundaries after cleanup.'
  artifacts:
    - path: packages/rts-engine/template-grid-authoritative.ts
      provides: authoritative placement evaluation helper surface shared by preview and execute-time revalidation
      contains: projection, legality reason derivation, diff/affordability evaluation, and parity-safe utility wiring
    - path: packages/rts-engine/rts.ts
      provides: orchestration-only runtime flow wired to shared authoritative geometry helpers
      contains: removed duplicate local geometry helper logic plus preserved rejection/resource guard ordering
    - path: packages/rts-engine/template-grid-authoritative.test.ts
      provides: deterministic helper-level parity and rejection-order regression coverage
      contains: representative transformed success and invalid scenario assertions
    - path: packages/rts-engine/rts.test.ts
      provides: engine-level migration parity assertions across preview/queue/execute boundaries
      contains: temporary old-vs-new guard assertions and reason/resource outcome checks
    - path: packages/rts-engine/index.ts
      provides: package export surface for new authoritative helper module
      contains: `template-grid-authoritative` exports for tests and engine consumers
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/template-grid-authoritative.ts
      via: preview and execute-time evaluation call one authoritative helper surface
      pattern: evaluateBuildPlacement|projectBuildPlacement|compareTemplate|applyTemplate
    - from: packages/rts-engine/template-grid-authoritative.test.ts
      to: packages/rts-engine/template-grid-authoritative.ts
      via: direct deterministic parity and reason-order assertions
      pattern: projection|diff|affordability|reason
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: end-to-end queue and tick assertions verify preserved authoritative outcomes
      pattern: previewBuildPlacement|queueBuildEvent|tickRoom
---

<objective>
Extract the remaining authoritative geometry cleanup work from `rts.ts` into one shared helper module so legacy template/offset-template duplication is removed without changing rejection semantics, affordability behavior, or transformed outcome determinism.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-CONTEXT.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-RESEARCH.md
@.planning/phases/16-write-path-gridview-unification/16-02-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/template-grid-write.ts
@packages/rts-engine/template-grid-read.ts
@packages/rts-engine/rts.test.ts
</context>

<tasks>
  <task type="auto">
    <name>Create a shared authoritative build-evaluation helper module</name>
    <files>
      - packages/rts-engine/template-grid-authoritative.ts
      - packages/rts-engine/template-grid-authoritative.test.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Add a dedicated helper module that composes canonical transformed write projection/diff/apply utilities into one authoritative build-evaluation surface used by runtime callers.
      Keep deterministic transformed ordering, torus wrapping semantics, and reason derivation compatibility with current behavior.
      Cover representative transformed success and invalid flows with helper-level tests that lock reason ordering and affordability semantics.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-authoritative.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      A shared authoritative helper API exists, is exported, and helper-level deterministic parity tests pass for representative flows.
    </done>
  </task>

  <task type="auto">
    <name>Rewire `rts.ts` preview and execute-time build evaluation to the shared helper</name>
    <files>
      - packages/rts-engine/rts.ts
      - packages/rts-engine/template-grid-authoritative.ts
    </files>
    <action>
      Replace remaining local geometry helper glue in `rts.ts` with calls to the new authoritative helper module and remove duplicate template/offset-template traversal remnants.
      Preserve guard ordering and reason taxonomy (`outside-territory`, `template-exceeds-map-size`, `template-compare-failed`, `occupied-site`, `insufficient-resources`) and keep execute-time charge timing unchanged.
      Ensure structure key derivation and accepted-event projection payloads remain behaviorally identical for equivalent transformed inputs.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps preview and queue parity for canonical transformed placements"
        - npx vitest run packages/rts-engine/rts.test.ts -t "preserves occupied-site precedence over insufficient resources at execute time"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      `rts.ts` no longer carries duplicate authoritative geometry logic, and targeted parity tests confirm unchanged rejection/resource semantics.
    </done>
  </task>

  <task type="auto">
    <name>Add temporary migration parity assertions for representative action boundaries</name>
    <files>
      - packages/rts-engine/rts.test.ts
      - packages/rts-engine/template-grid-authoritative.test.ts
    </files>
    <action>
      Add temporary old-vs-new migration assertions in deterministic test scenarios that compare preview, queue, and execute-time outcomes for representative success and invalid flows.
      Assert action-boundary checkpoints (reason, affordability metadata, resources, and structure footprint/key outcomes) so parity drift is caught immediately.
      Mark temporary assertions with explicit Phase 18 cleanup intent so they can be removed once parity closure is complete.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-authoritative.test.ts packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Temporary migration parity assertions are active, deterministic, and clearly scoped for retirement in Phase 18.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/template-grid-authoritative.test.ts`
- `npx vitest run packages/rts-engine/rts.test.ts -t "keeps preview and queue parity for canonical transformed placements"`
- `npx vitest run packages/rts-engine/rts.test.ts -t "preserves occupied-site precedence over insufficient resources at execute time"`
- `npx vitest run packages/rts-engine/template-grid-authoritative.test.ts packages/rts-engine/rts.test.ts`

Confirm:

- authoritative build evaluation is centralized and duplicate geometry helper logic is removed from `rts.ts`
- rejection taxonomy, affordability metadata, and resource charging behavior remain parity-stable
- representative transformed success/invalid flows remain deterministic across preview, queue, and execute boundaries
  </verification>

<success_criteria>

- Requirement `REF-06` cleanup is implemented: duplicate authoritative geometry logic is removed from `rts.ts`.
- Authoritative runtime behavior remains parity-stable for reason taxonomy, charging semantics, and transformed outcomes.
- Temporary migration assertions exist to guard remaining parity risk until Phase 18 closure.
  </success_criteria>

<output>
Create `.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-01-SUMMARY.md` with helper extraction details, parity evidence, and temporary assertion cleanup notes for Phase 18.
</output>
