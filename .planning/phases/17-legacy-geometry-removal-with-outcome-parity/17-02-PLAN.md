---
phase: 17-legacy-geometry-removal-with-outcome-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - packages/rts-engine/rts.test.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
  - tests/integration/server/destroy-determinism.test.ts
  - tests/integration/server/match-lifecycle.test.ts
autonomous: true
requirements:
  - REF-06
must_haves:
  truths:
    - 'Representative transformed match flows keep the same authoritative accept/reject outcomes after legacy geometry cleanup.'
    - 'Equivalent invalid actions keep rejection reason taxonomy and action-timeline cadence parity without side-effect drift.'
    - 'Deterministic reruns of the same action timelines preserve required intermediate checkpoints and final resource/structure state.'
  artifacts:
    - path: packages/rts-engine/rts.test.ts
      provides: engine-level representative timeline parity matrix for success and invalid transformed flows
      contains: action-boundary checkpoint assertions for reasons, affordability metadata, resources, and structure footprint/key outcomes
    - path: tests/integration/server/server.test.ts
      provides: socket-contract parity assertions for preview, queue, and outcome semantics
      contains: transformed invalid-action reason taxonomy and ordering checks at runtime boundary
    - path: tests/integration/server/quality-gate-loop.test.ts
      provides: deterministic quality-loop parity coverage for repeated invalid attempts and execute-time rejects
      contains: cadence and no-side-effect regression checks across representative transformed scenarios
    - path: tests/integration/server/destroy-determinism.test.ts
      provides: transformed structure-key and destroy-target parity evidence after cleanup
      contains: occupied-site and destroy-target determinism checks tied to cleaned authoritative projection path
    - path: tests/integration/server/match-lifecycle.test.ts
      provides: representative end-to-end timeline parity guard across match progression boundaries
      contains: ordering and outcome parity checks for queued actions through terminal lifecycle transitions
  key_links:
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: deterministic action-by-action assertions verify preserved authoritative outcomes
      pattern: previewBuildPlacement|queueBuildEvent|tickRoom
    - from: tests/integration/server/server.test.ts
      to: apps/server/src/server.ts
      via: socket event assertions validate unchanged rejection semantics and cadence
      pattern: build:preview|build:queue|build:outcome|room:error
    - from: tests/integration/server/destroy-determinism.test.ts
      to: packages/rts-engine/rts.ts
      via: transformed structure-key stability checks confirm cleanup did not alter occupied-site and destroy behavior
      pattern: structureKey|occupied-site|invalid-target
---

<objective>
Prove Phase 17 outcome parity with representative deterministic unit and integration scenarios so legacy geometry cleanup is sign-off safe for authoritative accept/reject behavior, rejection cadence, and resource/structure checkpoints.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-CONTEXT.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-RESEARCH.md
@.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-01-PLAN.md
@packages/rts-engine/rts.test.ts
@tests/integration/server/server.test.ts
@tests/integration/server/quality-gate-loop.test.ts
@tests/integration/server/destroy-determinism.test.ts
@tests/integration/server/match-lifecycle.test.ts
@apps/server/src/server.ts
</context>

<tasks>
  <task type="auto">
    <name>Expand representative engine parity scenarios with action-boundary checkpoints</name>
    <files>
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Add deterministic representative scenario matrix coverage for transformed success paths and invalid/reject paths after cleanup.
      Assert required checkpoints after each action boundary (preview, queue, tick/outcome) including rejection reason, affordability metadata, resources, and structure footprint/key state.
      Include repeated invalid-attempt scenarios to lock rejection cadence and no-side-effect invariants.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps transformed preview footprint aligned with applied structure footprint coordinates"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Unit-level representative parity checks fail on any action-boundary drift in outcomes, reasons, or state checkpoints.
    </done>
  </task>

  <task type="auto">
    <name>Harden socket-level rejection taxonomy and cadence parity assertions</name>
    <files>
      - tests/integration/server/server.test.ts
      - tests/integration/server/quality-gate-loop.test.ts
      - apps/server/src/server.ts
    </files>
    <action>
      Extend integration scenarios to assert equivalent transformed invalid actions keep the same reason taxonomy and event ordering (`build:preview`, `build:queue`, `build:outcome`, and `room:error`) after cleanup.
      Verify repeated invalid attempts keep rejection cadence parity and do not mutate authoritative resource/structure state.
      Limit runtime changes to parity-preserving fixes only if integration evidence reveals a drift introduced by cleanup.
    </action>
    <verify>
      <automated>
        - npx vitest run tests/integration/server/server.test.ts -t "preserves rejection taxonomy and cadence for equivalent transformed invalid queues"
        - npx vitest run tests/integration/server/quality-gate-loop.test.ts -t "keeps equivalent transform legality parity and execute-time affordability rejections stable"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Runtime contract parity is locked for transformed invalid-action semantics and rejection cadence after authoritative cleanup.
    </done>
  </task>

  <task type="auto">
    <name>Prove deterministic rerun parity for structure and resource checkpoints</name>
    <files>
      - tests/integration/server/destroy-determinism.test.ts
      - tests/integration/server/match-lifecycle.test.ts
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Add rerun-based deterministic assertions for representative timelines that compare required intermediate checkpoints and final state across repeated executions.
      Verify transformed structure-key and destroy-target behavior remain identical for equivalent timelines after duplicate-path removal.
      Capture parity evidence for both successful build flows and invalid/reject-heavy flows so one mismatch blocks sign-off.
    </action>
    <verify>
      <automated>
        - npx vitest run tests/integration/server/destroy-determinism.test.ts -t "keeps transformed structure keys stable for occupied-site and destroy targeting checks"
        - npx vitest run tests/integration/server/match-lifecycle.test.ts -t "keeps queued action outcome ordering deterministic across reruns"
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Deterministic reruns preserve required intermediate and final checkpoints for representative transformed timelines.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/rts.test.ts -t "keeps representative action-timeline parity after legacy geometry cleanup"`
- `npx vitest run tests/integration/server/server.test.ts -t "preserves rejection taxonomy and cadence for equivalent transformed invalid queues"`
- `npx vitest run tests/integration/server/quality-gate-loop.test.ts -t "keeps equivalent transform legality parity and execute-time affordability rejections stable"`
- `npx vitest run tests/integration/server/destroy-determinism.test.ts -t "keeps transformed structure keys stable for occupied-site and destroy targeting checks"`
- `npx vitest run tests/integration/server/match-lifecycle.test.ts -t "keeps queued action outcome ordering deterministic across reruns"`

Confirm:

- representative transformed success and invalid flows keep authoritative accept/reject parity
- rejection reason taxonomy and timeline cadence stay stable for equivalent invalid attempts
- deterministic reruns preserve required resource/structure checkpoints and final state outcomes
  </verification>

<success_criteria>

- Requirement `REF-06` is proven by representative parity evidence at engine and runtime boundaries.
- Cleanup introduces no drift in rejection semantics, action cadence, or deterministic state checkpoints.
- Phase 17 exits with sign-off-ready parity evidence for Phase 18 migration-assertion retirement work.
  </success_criteria>

<output>
Create `.planning/phases/17-legacy-geometry-removal-with-outcome-parity/17-02-SUMMARY.md` with representative parity evidence, deterministic checkpoint results, and any residual risks to carry into Phase 18.
</output>
