---
phase: 15-read-path-and-cross-codebase-gridview-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/template-grid-read.ts
  - packages/rts-engine/template-grid-read.test.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - packages/rts-engine/index.ts
autonomous: true
requirements:
  - REF-05
must_haves:
  truths:
    - 'Structure projection, build-zone contributor projection inputs, and integrity-mask reads come from one shared GridView-backed utility path.'
    - 'For identical transform inputs, projected structure footprint cells and integrity check cells remain stable across repeated runs and reconnect snapshots.'
    - 'Read-side geometry logic is no longer duplicated across private helper forks inside `rts.ts`.'
  artifacts:
    - path: packages/rts-engine/template-grid-read.ts
      provides: shared read-side transformed-grid projection primitives
      contains: template transform projection, world projection helpers, transformed bounds helper, integrity mask derivation helpers
    - path: packages/rts-engine/rts.ts
      provides: authoritative engine read-path wiring through shared helper APIs
      contains: structure projection, build-zone contributor projection inputs, and integrity-mask readers migrated to shared utilities
    - path: packages/rts-engine/template-grid-read.test.ts
      provides: deterministic parity coverage for shared projection helpers
      contains: rotate/mirror matrix scenarios, deterministic ordering assertions, transformed bounds parity checks
    - path: packages/rts-engine/rts.test.ts
      provides: regression coverage for read-path behavior parity
      contains: structure payload footprint and integrity/read parity assertions after helper migration
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/template-grid-read.ts
      via: read-side consumers call one shared projection utility instead of local helper forks
      pattern: projectStructures|collectTeamBuildZoneContributors|getIntegrityMaskCells
    - from: packages/rts-engine/template-grid-read.test.ts
      to: packages/rts-engine/template-grid-read.ts
      via: direct helper parity assertions for transform matrix and bounds behavior
      pattern: transform|bounds|project
---

<objective>
Extract and adopt one shared GridView-backed read projection utility so structure projection, build-zone contributor projection inputs, and integrity-mask reads all preserve current observable behavior while eliminating duplicated in-file geometry logic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-read-path-and-cross-codebase-gridview-unification/15-CONTEXT.md
@.planning/phases/15-read-path-and-cross-codebase-gridview-unification/15-RESEARCH.md
@.planning/phases/14-canonical-gridview-api-adoption/14-02-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/rts.test.ts
@packages/rts-engine/grid-view.ts
@packages/rts-engine/placement-transform.ts
@packages/rts-engine/index.ts
</context>

<tasks>
  <task type="auto">
    <name>Create shared read-path GridView projection module</name>
    <files>
      - packages/rts-engine/template-grid-read.ts
      - packages/rts-engine/template-grid-read.test.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Add a dedicated shared module that owns read-side transformed-template projection primitives currently embedded in `rts.ts`.
      Include helpers for template-to-GridView transform projection, world projection/wrapping, and transformed bounds estimation suitable for cross-codebase helper usage.
      Keep helper outputs deterministic and parity-safe with existing `rts.ts` observable behavior.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-read.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      A shared exported helper surface exists for read-side transformed-grid projection and passes dedicated parity tests.
    </done>
  </task>

  <task type="auto">
    <name>Refactor engine read-side consumers to use shared projection helpers</name>
    <files>
      - packages/rts-engine/rts.ts
      - packages/rts-engine/template-grid-read.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Migrate `projectStructures`, build-zone contributor projection input derivation, and integrity mask read paths to call the new shared helper APIs.
      Preserve parity with current player-visible behavior (structure footprint cells, transformed dimensions, and integrity checks) and avoid write-path semantic changes in this plan.
      Remove now-redundant local read helper forks from `rts.ts` once equivalent shared paths are wired.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      REF-05 read consumers in `rts.ts` are routed through one shared GridView-backed helper path with unchanged observable outcomes.
    </done>
  </task>

  <task type="auto">
    <name>Add deterministic parity regressions for shared read-path outputs</name>
    <files>
      - packages/rts-engine/template-grid-read.test.ts
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Add regression cases that cover rotate/mirror transform matrices, sparse and dense templates, and repeated identical transform sequences.
      Assert stable ordering and parity for projected footprint/check cells used by structure payload projection and integrity-mask reads.
      Include assertions that guard against reconnect-visible wobble caused by ordering or projection drift.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-read.test.ts packages/rts-engine/rts.test.ts
        - npm run test:unit
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Deterministic parity coverage fails on read-path transform drift and protects the migration against regressions.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/template-grid-read.test.ts packages/rts-engine/rts.test.ts`
- `npm run test:unit`

Confirm:

- structure payload footprints match pre-migration behavior for equivalent transform inputs
- build-zone contributor projection inputs still map to the same transformed width/height outputs
- integrity-mask read paths use shared helpers and preserve expected transformed check cells
  </verification>

<success_criteria>

- Requirement `REF-05` is satisfied through one shared read-side GridView projection path.
- Read-path transformed-cell outputs remain deterministic and reconnect-stable.
- Duplicate read-side transform helper forks in `rts.ts` are removed or reduced to thin adapters.
  </success_criteria>

<output>
Create `.planning/phases/15-read-path-and-cross-codebase-gridview-unification/15-01-SUMMARY.md` with migration details, parity evidence, and any discovered ambiguities.
</output>
