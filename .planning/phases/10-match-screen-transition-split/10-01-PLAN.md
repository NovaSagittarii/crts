---
phase: 10-match-screen-transition-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/index.html
  - apps/web/src/client.ts
  - apps/web/src/match-screen-view-model.ts
  - tests/web/match-screen-view-model.test.ts
autonomous: true
requirements:
  - UI-01
must_haves:
  truths:
    - 'Players see a dedicated lobby screen for `lobby` and `countdown` states, and lobby controls remain available there.'
    - 'Players see a dedicated in-game screen for `active` and `finished` states, and lobby-only controls are hidden there.'
    - 'Screen switches happen only after authoritative lifecycle state updates from server events, not from local intent clicks.'
    - 'Reconnecting players briefly see a syncing indicator, then land on the authoritative screen for current status with neutral confirmation copy.'
    - 'Unsent chat draft text survives authoritative screen transitions.'
  artifacts:
    - path: apps/web/src/match-screen-view-model.ts
      provides: Authoritative status-to-screen mapping and transition/reconnect banner state helpers
      contains: resolveScreenForStatus, deduped status transition logic, reconnect syncing and confirmation copy state
    - path: tests/web/match-screen-view-model.test.ts
      provides: Deterministic regression coverage for screen mapping and reconnect messaging behavior
      contains: lobby/countdown vs active/finished mapping, status-change dedupe, reconnect notice flow
    - path: apps/web/index.html
      provides: Split lobby and in-game screen containers with shared right-docked chat and edge status/banner surfaces
      contains: lobby screen container, in-game screen container, edge banner, reconnect indicator, 150-250ms fade classes
    - path: apps/web/src/client.ts
      provides: Authoritative lifecycle event wiring into screen transitions without local override mode
      contains: view-model integration, authoritative status handling, reconnect landing resolution, persistent chat draft behavior
  key_links:
    - from: apps/web/src/client.ts
      to: apps/web/src/match-screen-view-model.ts
      via: authoritative room-status application and transition/reconnect notice derivation
      pattern: resolveScreenForStatus|applyAuthoritativeStatus|pendingReconnect
    - from: apps/web/src/client.ts
      to: apps/web/index.html
      via: lobby/in-game visibility toggles plus shared edge banner and reconnect indicator bindings
      pattern: lobby-screen|ingame-screen|edge-banner|reconnect-indicator
    - from: apps/web/src/client.ts
      to: packages/rts-engine/socket-contract.ts
      via: room status updates consumed from membership and lifecycle events
      pattern: room:membership|room:countdown|room:match-started|room:match-finished
---

<objective>
Split the current dual-purpose web layout into explicit lobby and in-game screens that route strictly from authoritative match status.

Purpose: Satisfy UI-01 by removing local screen overrides and making reconnect landing deterministic and status-driven.
Output: A reusable match-screen view-model, split screen layout, authoritative transition wiring, and focused web regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-match-screen-transition-split/10-CONTEXT.md
@.planning/phases/10-match-screen-transition-split/10-RESEARCH.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-03-SUMMARY.md
@apps/web/index.html
@apps/web/src/client.ts
@apps/web/src/destroy-view-model.ts
@apps/web/src/placement-transform-view-model.ts
@tests/web/placement-transform-view-model.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add authoritative match-screen view-model and deterministic tests</name>
  <files>
    apps/web/src/match-screen-view-model.ts
    tests/web/match-screen-view-model.test.ts
  </files>
  <action>
    Create a dedicated match-screen view-model module (same pure-helper style as existing web view-model modules) that maps authoritative `RoomStatus` to visible screen (`lobby` for `lobby`/`countdown`, `ingame` for `active`/`finished`), computes whether a transition banner should fire when status actually changes, and models reconnect messaging state (`Reconnecting / syncing...` before authoritative resolution, then `Reconnected. Synced to match state.` for ~2-3s once resolved). Keep this module purely state-based and deterministic; do not read DOM or socket state directly inside it.
  </action>
  <verify>
    <automated>npx vitest run tests/web/match-screen-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>New view-model helpers and tests lock status-to-screen mapping, status-change dedupe, and reconnect notice sequencing behavior.</done>
</task>

<task type="auto">
  <name>task 2: split layout into lobby and in-game screens with shared chat/status shell</name>
  <files>
    apps/web/index.html
  </files>
  <action>
    Restructure the page into two explicit screen containers (`lobby` and `in-game`) while keeping one persistent right-docked chat surface and one shared edge status strip outside both screens so they remain visible in both modes. Keep lobby controls (slot claim, ready toggle, host start, roster, spectators) in the lobby screen only; keep gameplay-centric controls and board in the in-game screen only; keep finished results in in-game context. Add compact edge banner/reconnect indicator elements and CSS fade transitions in the 150-250ms window for screen switches. Preserve responsive behavior and minimize scrolling by keeping primary controls and shared status surfaces visible without modal takeovers.
  </action>
  <verify>
    <automated>npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>DOM and styles support dedicated lobby/in-game screen regions, shared right-side chat, shared edge status strip, and short transition/banner surfaces.</done>
</task>

<task type="auto">
  <name>task 3: rewire client transitions to authoritative status only and remove local override paths</name>
  <files>
    apps/web/src/client.ts
    apps/web/index.html
    apps/web/src/match-screen-view-model.ts
  </files>
  <action>
    Integrate the new match-screen view-model into `client.ts` so authoritative lifecycle updates (`room:membership`, `room:countdown`, `room:match-started`, `room:match-finished`) are the only source of screen changes. Remove local override behavior (including finished "Return to Lobby View" toggles) and stop forcing `lobby` on join/reconnect before authoritative membership status arrives. Apply the reconnect landing map exactly (`lobby`/`countdown` -> lobby screen, `active`/`finished` -> in-game screen), show the required neutral confirmation copy after first authoritative sync, and keep compact status-change banners for ~2-3s with deduped status-delta handling. Preserve unsent chat draft by keeping a single persistent chat input node and by avoiding focus-forcing logic during transitions.
  </action>
  <verify>
    <automated>npx vitest run tests/web/match-screen-view-model.test.ts tests/web/destroy-view-model.test.ts && npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Client navigation is strictly authoritative, reconnect landing is deterministic, local override mode is removed, and chat drafts survive screen transitions.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/match-screen-view-model.test.ts tests/web/destroy-view-model.test.ts tests/web/placement-transform-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- UI-01 is true: lobby and in-game are separate dedicated screens instead of one dual-purpose layout.
- `countdown` resolves to lobby and `finished` resolves to in-game results until host-driven restart changes authoritative status.
- Screen transitions occur only after authoritative status events, with no local screen override mode remaining.
- Reconnect flow shows syncing indicator, then lands on the correct authoritative screen with the exact neutral confirmation message.
  </success_criteria>

<output>
After completion, create `.planning/phases/10-match-screen-transition-split/10-01-SUMMARY.md`
</output>
