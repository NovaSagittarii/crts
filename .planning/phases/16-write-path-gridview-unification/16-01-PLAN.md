---
phase: 16-write-path-gridview-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/template-grid-write.ts
  - packages/rts-engine/template-grid-write.test.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/index.ts
autonomous: true
requirements:
  - REF-04
must_haves:
  truths:
    - 'Preview legality, queue revalidation, and execute-time apply all consume one canonical GridView-backed write projection for the same anchor and transform input.'
    - 'Rejection precedence and reason taxonomy remain deterministic and unchanged while write-path helpers are unified.'
    - 'Accepted transformed builds apply to the exact wrapped world coordinates represented by the write projection used during validation.'
  artifacts:
    - path: packages/rts-engine/template-grid-write.ts
      provides: shared write-path GridView projection, diff, and apply helper surface
      contains: transformed world-cell stream helpers, legality projection data, diff counting, and apply mutation adapters
    - path: packages/rts-engine/rts.ts
      provides: authoritative queue/preview/apply orchestration wired through shared write helpers
      contains: projectBuildPlacement and evaluateBuildPlacement migration plus tick apply path using shared write projection outputs
    - path: packages/rts-engine/template-grid-write.test.ts
      provides: deterministic helper-level parity coverage for transformed write traversal
      contains: seam-wrap, equivalent-orientation legality, and compare/apply parity assertions
    - path: packages/rts-engine/index.ts
      provides: shared helper export surface for downstream runtime/tests
      contains: template-grid-write exports needed by package consumers
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/template-grid-write.ts
      via: preview/queue evaluation and tick apply call one shared write helper path
      pattern: projectBuildPlacement|evaluateBuildPlacement|applyTemplate
    - from: packages/rts-engine/template-grid-write.test.ts
      to: packages/rts-engine/template-grid-write.ts
      via: direct helper parity assertions for transformed world-cell traversal and diff/apply equivalence
      pattern: transform|wrap|compare|apply
---

<objective>
Extract and adopt one canonical GridView-backed write helper module so preview legality, queue revalidation, and execute-time apply all use the same transformed geometry source without changing existing rejection behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-write-path-gridview-unification/16-CONTEXT.md
@.planning/phases/16-write-path-gridview-unification/16-RESEARCH.md
@.planning/phases/15-read-path-and-cross-codebase-gridview-unification/15-01-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/template-grid-read.ts
@packages/rts-engine/placement-transform.ts
@packages/rts-engine/index.ts
</context>

<tasks>
  <task type="auto">
    <name>Create shared GridView-backed write-path helper module</name>
    <files>
      - packages/rts-engine/template-grid-write.ts
      - packages/rts-engine/template-grid-write.test.ts
      - packages/rts-engine/index.ts
    </files>
    <action>
      Add a dedicated write-side helper module that centralizes transformed world-cell traversal for legality projection, diff-cost calculation, and apply mutation.
      Mirror deterministic ordering and torus wrapping semantics used by existing preview/queue/apply behavior.
      Keep overlap behavior unchanged (writes still overwrite existing cells) and preserve transformed bounds semantics used by structure-key derivation.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-write.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      A shared write-helper surface exists and helper-level parity tests prove deterministic transformed traversal behavior.
    </done>
  </task>

  <task type="auto">
    <name>Migrate authoritative preview/queue/apply write consumers to shared helpers</name>
    <files>
      - packages/rts-engine/rts.ts
      - packages/rts-engine/template-grid-write.ts
    </files>
    <action>
      Refactor `projectBuildPlacement`, `evaluateBuildPlacement`, execute-time queue revalidation, and tick apply mutation paths to call the shared write helper APIs.
      Preserve current rejection precedence and codes (`outside-territory`, `template-exceeds-map-size`, `template-compare-failed`, `occupied-site`, `insufficient-resources`) and keep resource deductions in execute-time accepted paths only.
      Keep `createStructureKey(x,y,width,height)` inputs consistent with existing transformed bounds behavior to avoid occupied-site and destroy-target drift.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts -t "keeps preview and queue parity for canonical transformed placements"
        - npx vitest run packages/rts-engine/rts.test.ts -t "accepts torus-wrapped placements and rejects transformed templates that exceed map size"
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      RTS write-path orchestration uses one shared GridView-backed geometry source and preserves existing reason/resource semantics.
    </done>
  </task>

  <task type="auto">
    <name>Add regression guards for write-path helper parity at engine boundary</name>
    <files>
      - packages/rts-engine/template-grid-write.test.ts
      - packages/rts-engine/rts.ts
    </files>
    <action>
      Add deterministic regression assertions covering seam-crossing placements, equivalent-orientation legality parity, and compare/apply traversal equivalence at helper and engine boundary layers.
      Include temporary migration assertions only where needed to confirm old-vs-new write traversal parity, and keep them scoped for later cleanup phases.
      Ensure every regression remains deterministic and independent of runtime/socket state.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/template-grid-write.test.ts packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Deterministic tests fail on write-path transformed traversal drift before downstream runtime integration work begins.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/template-grid-write.test.ts packages/rts-engine/rts.test.ts`

Confirm:

- preview legality and execute apply consume one canonical transformed write projection path
- transformed seam crossings and equivalent orientations keep parity outcomes
- rejection precedence and structure-key inputs are unchanged after helper migration
  </verification>

<success_criteria>

- Requirement `REF-04` write-path helper unification is implemented at the engine level.
- Preview, queue, and apply now share one deterministic GridView-backed geometry source.
- Reason taxonomy, resource timing, and transformed key semantics remain behaviorally stable.
  </success_criteria>

<output>
Create `.planning/phases/16-write-path-gridview-unification/16-01-SUMMARY.md` with helper migration details, parity evidence, and any temporary migration assertions introduced.
</output>
