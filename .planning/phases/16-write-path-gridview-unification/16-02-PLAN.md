---
phase: 16-write-path-gridview-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - packages/rts-engine/rts.test.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
  - tests/integration/server/destroy-determinism.test.ts
autonomous: true
requirements:
  - REF-04
must_haves:
  truths:
    - 'For equivalent placement input and transform orientation, preview and queue validation return matching accept/reject behavior with unchanged rejection reasons.'
    - 'Accepted builds apply to the same transformed wrapped coordinates represented in authoritative preview footprint payloads.'
    - 'When state changes invalidate queued actions, execute-time rejections preserve current-state reason and affordability metadata without charging resources.'
  artifacts:
    - path: packages/rts-engine/rts.test.ts
      provides: deterministic unit parity matrix for write-path transformed legality/apply/resource behavior
      contains: preview->queue->apply parity fixtures, rejection precedence checks, and execute-time no-charge rejection assertions
    - path: tests/integration/server/server.test.ts
      provides: socket-level write-path contract parity coverage
      contains: transformed preview/queue/outcome parity checks plus rejection/refresh payload assertions
    - path: tests/integration/server/quality-gate-loop.test.ts
      provides: deterministic quality-loop scenarios covering transformed write-path edge behavior
      contains: equivalent-orientation legality parity and current-state rejection outcome checks in full match flows
    - path: tests/integration/server/destroy-determinism.test.ts
      provides: structure-key and overwrite behavior parity under transformed write flows
      contains: occupied-site/destroy targeting assertions tied to transformed placement key stability
  key_links:
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: deterministic assertions validate shared write projection behavior and execute-time charging semantics
      pattern: previewBuildPlacement|queueBuildEvent|tickRoom
    - from: tests/integration/server/server.test.ts
      to: apps/server/src/server.ts
      via: socket contract assertions verify reason/affordability payload parity for transformed preview/queue/outcome flows
      pattern: build:preview|build:queue|build:outcome|room:error
    - from: tests/integration/server/destroy-determinism.test.ts
      to: packages/rts-engine/rts.ts
      via: transformed placement key stability assertions protect occupied-site and destroy targeting parity
      pattern: structureKey|occupied-site|invalid-target
---

<objective>
Lock write-path parity end to end by proving transformed preview, queue validation, and execute apply stay aligned on coordinates, rejection reasons, and affordability/resource outcomes across deterministic unit and integration scenarios.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-write-path-gridview-unification/16-CONTEXT.md
@.planning/phases/16-write-path-gridview-unification/16-RESEARCH.md
@.planning/phases/16-write-path-gridview-unification/16-01-PLAN.md
@packages/rts-engine/rts.test.ts
@tests/integration/server/server.test.ts
@tests/integration/server/quality-gate-loop.test.ts
@tests/integration/server/destroy-determinism.test.ts
@apps/server/src/server.ts
</context>

<tasks>
  <task type="auto">
    <name>Expand engine unit parity matrix for transformed write-path outcomes</name>
    <files>
      - packages/rts-engine/rts.test.ts
    </files>
    <action>
      Add deterministic unit scenarios that assert transformed preview footprint/bounds parity with queued acceptance and applied structure footprint coordinates.
      Add rejection precedence tests that preserve the existing top-reason ordering when multiple legality constraints fail.
      Add execute-time revalidation tests confirming no resource deduction occurs when previously queueable actions are rejected at apply time.
    </action>
    <verify>
      <automated>
        - npx vitest run packages/rts-engine/rts.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Engine unit coverage fails on any transformed write-path parity drift in coordinates, reasons, or resource timing.
    </done>
  </task>

  <task type="auto">
    <name>Add server integration assertions for transformed preview/queue/outcome parity</name>
    <files>
      - tests/integration/server/server.test.ts
      - apps/server/src/server.ts
    </files>
    <action>
      Extend socket integration coverage to assert transformed preview payloads (`transform`, `bounds`, `footprint`) remain aligned with queue acceptance/rejection and terminal outcomes.
      Assert rejected queue requests still trigger a refreshed authoritative preview for the same anchor+transform so stale preview state is cleared.
      Verify current-state rejection reasons and affordability metadata remain stable for execute-time rejects after state drift.
      Keep existing rejection reason wording/taxonomy unchanged and only adjust runtime wiring if integration evidence shows refresh or parity gaps.
    </action>
    <verify>
      <automated>
        - npx vitest run tests/integration/server/server.test.ts
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Cross-runtime transformed write-path behavior is contract-locked for preview, queue, rejection-refresh, and outcome parity.
    </done>
  </task>

  <task type="auto">
    <name>Harden deterministic edge scenarios for structure-key and torus parity</name>
    <files>
      - tests/integration/server/quality-gate-loop.test.ts
      - tests/integration/server/destroy-determinism.test.ts
    </files>
    <action>
      Add/adjust deterministic match-loop scenarios that compare equivalent transform orientations and seam-crossing placements for identical legality outcomes.
      Lock structure-key stability assertions so transformed occupied-site checks and destroy targeting remain unchanged.
      Preserve overlap-by-overwrite behavior and ensure no out-of-bounds rejection path is introduced for torus seam placements.
    </action>
    <verify>
      <automated>
        - npx vitest run tests/integration/server/quality-gate-loop.test.ts tests/integration/server/destroy-determinism.test.ts
        - npm run test:integration:serial
      </automated>
      <sampling_rate>100%</sampling_rate>
    </verify>
    <done>
      Deterministic integration gates enforce transformed key/torus/reason parity for write-path behavior.
    </done>
  </task>
</tasks>

<verification>
Run:
- `npx vitest run packages/rts-engine/rts.test.ts`
- `npx vitest run tests/integration/server/server.test.ts tests/integration/server/quality-gate-loop.test.ts tests/integration/server/destroy-determinism.test.ts`
- `npm run test:integration:serial`

Confirm:

- transformed preview legality and queue acceptance/rejection parity hold across representative orientation matrix inputs
- accepted builds apply to the same transformed wrapped coordinates represented in preview payloads
- execute-time current-state rejects keep reason/resource metadata parity and never charge resources
  </verification>

<success_criteria>

- Requirement `REF-04` is proven with deterministic unit + integration parity evidence.
- Rejection reason precedence, affordability metadata, and resource charge timing remain unchanged.
- Torus seam behavior, overlap overwrite semantics, and transformed structure-key stability are preserved.
  </success_criteria>

<output>
Create `.planning/phases/16-write-path-gridview-unification/16-02-SUMMARY.md` with parity matrix evidence, runtime contract outcomes, and any residual migration gaps for Phase 17 follow-up.
</output>
