---
phase: 05-quality-gate-validation
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/server/quality-gate-loop.test.ts
  - package.json
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - Developers can run one explicit integration scenario that proves join -> build -> tick -> breach -> defeat behavior.
    - Developers can run the integration quality gate in default mode and a deterministic serial fallback mode.
    - Developers can run one command that executes both unit and integration quality gates before broader feature work.
  artifacts:
    - path: tests/integration/server/quality-gate-loop.test.ts
      provides: Requirement-tagged end-to-end integration scenario for the full match loop.
    - path: package.json
      provides: Quality-gate npm scripts for full and serial test execution.
  key_links:
    - from: tests/integration/server/quality-gate-loop.test.ts
      to: apps/server/src/server.ts
      via: Real Socket.IO clients drive room lifecycle and assert `build:queued`, `build:outcome`, `room:match-finished`, and defeated rejections.
      pattern: createServer|room:start|build:queue|build:outcome|room:match-finished|room:error
    - from: package.json
      to: vitest.config.ts
      via: Scripts route to Vitest integration scope with optional serial fallback for repeatability.
      pattern: test:integration|test:integration:serial|test:quality|--no-file-parallelism
---

<objective>
Lock QUAL-02 with an explicit end-to-end integration loop and script-level quality gates developers can run before expanding scope.

Purpose: Ensure the join-to-defeat multiplayer flow is continuously verifiable with deterministic automated commands.
Output: A requirement-tagged full-loop integration test plus npm quality-gate scripts for standard and serial execution.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quality-gate-validation/05-RESEARCH.md
@tests/integration/server/server.test.ts
@tests/integration/server/match-lifecycle.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>task 1: add an explicit QUAL-02 end-to-end loop integration test</name>
  <files>tests/integration/server/quality-gate-loop.test.ts</files>
  <action>Create a focused integration test file with a clearly labeled QUAL-02 scenario that runs the full sequence: host/guest join, both claim slots and ready up, match starts, at least one valid build is queued and reaches terminal outcome, breach path resolves to `room:match-finished`, and the defeated side receives `room:error` with `reason: defeated` when attempting another build. Use ephemeral ports, register event listeners before emits, and ensure teardown always closes sockets before stopping the server.</action>
  <verify>
    <automated>npx vitest run tests/integration/server/quality-gate-loop.test.ts</automated>
  </verify>
  <done>The repository has a single explicit QUAL-02 integration scenario that proves join -> build -> tick -> breach -> defeat and passes consistently.</done>
</task>

<task type="auto">
  <name>task 2: add quality-gate npm scripts for default and serial integration execution</name>
  <files>package.json</files>
  <action>Add script-level quality gates without replacing existing commands: keep `test:unit` and `test:integration` as-is, add `test:integration:serial` using `--no-file-parallelism` for deterministic fallback, and add `test:quality` that runs unit then integration gates in sequence. Do not introduce new tooling or coverage dependencies in this phase.</action>
  <verify>
    <automated>npm run test:integration:serial</automated>
  </verify>
  <done>`package.json` exposes `test:integration:serial` and `test:quality`, and both resolve through the existing Vitest setup.</done>
</task>

<task type="auto">
  <name>task 3: run QUAL-02 and full quality-gate commands together</name>
  <files>tests/integration/server/quality-gate-loop.test.ts, package.json</files>
  <action>Execute the new explicit QUAL-02 test, then run full integration and combined quality commands. If failures appear, stabilize only deterministic test orchestration (bounded waits, listener-before-emit ordering, cleanup in all paths) without changing gameplay rules or Socket.IO contract semantics.</action>
  <verify>
    <automated>npx vitest run tests/integration/server/quality-gate-loop.test.ts && npm run test:integration && npm run test:quality</automated>
  </verify>
  <done>QUAL-02 full-loop coverage and the combined quality-gate command both pass, proving repeatable end-to-end validation.</done>
</task>

</tasks>

<verification>
Run `npm run test:integration` and `npm run test:quality` and confirm the explicit `quality-gate-loop` scenario passes alongside existing integration coverage.
</verification>

<success_criteria>

- QUAL-02 has one explicit, requirement-tagged integration scenario for join/build/tick/breach/defeat.
- Integration gates are runnable in both default and serial modes using npm scripts.
- Developers can run `npm run test:quality` to execute the complete quality gate without manual command composition.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-quality-gate-validation/05-02-SUMMARY.md`
</output>
