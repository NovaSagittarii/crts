---
phase: 05-quality-gate-validation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/lobby.test.ts
  - packages/rts-engine/rts.test.ts
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - Developers can run unit tests with explicit QUAL-01 coverage for lobby/team invariants.
    - Developers can run unit tests with explicit QUAL-01 coverage for queue validation and terminal build outcomes.
    - Developers can run unit tests with explicit QUAL-01 coverage for economy affordability and income behavior.
  artifacts:
    - path: packages/rts-engine/lobby.test.ts
      provides: Requirement-tagged lobby/team invariant tests.
    - path: packages/rts-engine/rts.test.ts
      provides: Requirement-tagged queue validation, terminal outcome, and economy tests.
  key_links:
    - from: packages/rts-engine/lobby.test.ts
      to: packages/rts-engine/lobby.ts
      via: Lobby tests exercise create/join/claim/ready transitions and assert deterministic rejection reasons.
      pattern: createLobbyRoom|joinLobby|claimLobbySlot|setLobbyReady|reason
    - from: packages/rts-engine/rts.test.ts
      to: packages/rts-engine/rts.ts
      via: RTS tests exercise queueBuildEvent and tickRoom while asserting reason, outcome, and economy fields.
      pattern: queueBuildEvent|tickRoom|buildOutcomes|insufficient-resources|incomeBreakdown
---

<objective>
Make QUAL-01 unit coverage explicit and requirement-traceable so developers can prove lobby/team, queue, and economy invariants from one repeatable unit gate.

Purpose: Reduce ambiguity about whether current unit tests satisfy QUAL-01 domains.
Output: QUAL-tagged unit tests in `packages/rts-engine` that directly map to required quality-gate behaviors.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-quality-gate-validation/05-RESEARCH.md
@packages/rts-engine/lobby.test.ts
@packages/rts-engine/rts.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: make lobby and team invariant tests explicitly QUAL-01 scoped</name>
  <files>packages/rts-engine/lobby.test.ts</files>
  <action>Add a requirement-focused test section (or explicit test-name prefixes) for QUAL-01 lobby/team behavior. Cover spectator overflow, deterministic slot-claim contention, ready gating to player roles, and deterministic host transfer ordering. Keep existing edge-case assertions; do not replace them with broad smoke checks, because this gate must fail with domain-specific signals.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/lobby.test.ts</automated>
  </verify>
  <done>`packages/rts-engine/lobby.test.ts` contains explicit QUAL-01-labeled assertions for lobby/team invariants and passes.</done>
</task>

<task type="auto">
  <name>task 2: make queue validation, terminal outcomes, and economy tests explicitly QUAL-01 scoped</name>
  <files>packages/rts-engine/rts.test.ts</files>
  <action>Add or rename requirement-tagged tests that clearly cover all remaining QUAL-01 unit domains: queue validation rejections (`out-of-bounds`, `outside-territory`, `insufficient-resources` with numeric deficit fields), one terminal outcome per accepted queue event, and economy behavior from active/inactive structures. Assert typed fields (`reason`, `needed/current/deficit`, `outcome`) rather than message-string parsing.</action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
  </verify>
  <done>`packages/rts-engine/rts.test.ts` has explicit QUAL-01-labeled queue/outcome/economy coverage and all assertions pass.</done>
</task>

<task type="auto">
  <name>task 3: validate the repeatable QUAL-01 unit gate run</name>
  <files>packages/rts-engine/lobby.test.ts, packages/rts-engine/rts.test.ts</files>
  <action>Run the package unit gate end-to-end and address any non-deterministic assertions introduced by requirement tagging (for example, unstable ordering assumptions). Keep test setup isolated per test case so the gate stays repeatable for future quality checks.</action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <done>The full unit gate passes with explicit QUAL-01 mapping across lobby/team, queue validation, terminal outcomes, and economy rules.</done>
</task>

</tasks>

<verification>
Run `npm run test:unit` and confirm `packages/rts-engine/lobby.test.ts` + `packages/rts-engine/rts.test.ts` both report passing requirement-tagged QUAL-01 scenarios.
</verification>

<success_criteria>

- QUAL-01 unit domains are explicitly named and test-traceable in package unit suites.
- Queue validation, terminal outcomes, and economy coverage remain deterministic under repeated unit runs.
- Developers can use `npm run test:unit` as the documented QUAL-01 gate without interpretation.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-quality-gate-validation/05-01-SUMMARY.md`
</output>
