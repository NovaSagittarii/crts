---
phase: 11-camera-and-build-zone-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/build-zone.ts
  - packages/rts-engine/build-zone.test.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/index.ts
  - apps/web/src/camera-view-model.ts
  - tests/web/camera-view-model.test.ts
autonomous: true
requirements:
  - UI-02
  - UI-05
must_haves:
  truths:
    - 'Camera math keeps cursor-anchored zoom and pointer-to-cell conversion stable across the supported zoom window.'
    - 'Union-zone contributor projection uses the same transformed-structure center semantics as authoritative legality checks.'
    - 'Shared helper modules are deterministic and test-covered so Phase 11 web wiring can consume them without rule drift.'
  artifacts:
    - path: packages/rts-engine/build-zone.ts
      provides: Shared contributor and coverage helpers for union build-zone projection
      contains: contributor center projection, radius/shape coverage checks, illegal-cell collection helpers
    - path: packages/rts-engine/build-zone.test.ts
      provides: Deterministic unit coverage for contributor-center and union coverage semantics
      contains: inclusive radius boundary checks, multi-contributor union behavior, contributor removal regression
    - path: apps/web/src/camera-view-model.ts
      provides: Pure camera state transition and coordinate conversion helpers
      contains: zoom clamp constants, cursor-anchored zoom reducer, pan/reset reducers, inverse pointer mapping utilities
    - path: tests/web/camera-view-model.test.ts
      provides: Web helper tests proving camera precision and reset behavior
      contains: cursor anchor invariance, zoom clamp limits, pan updates, reset-to-base and spectator fallback behavior
  key_links:
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/build-zone.ts
      via: legality projection path for outside-territory decisions
      pattern: collectIllegalBuildZoneCells|collectBuildZoneContributors
    - from: apps/web/src/camera-view-model.ts
      to: tests/web/camera-view-model.test.ts
      via: deterministic reducer and conversion assertions
      pattern: applyWheelZoomAtPoint|screenPointToCell|resetCameraToBase
---

<objective>
Establish deterministic shared math primitives for camera transforms and union-zone geometry before wiring runtime input/render behavior.

Purpose: UI-02 and UI-05 both depend on coordinate-accurate transforms and parity-safe build-zone projection semantics that should be tested in isolation first.
Output: Shared engine build-zone helper, camera view-model helper, and focused unit/web tests locking boundary and precision behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-camera-and-build-zone-visualization/11-CONTEXT.md
@.planning/phases/11-camera-and-build-zone-visualization/11-RESEARCH.md
@packages/rts-engine/gameplay-rules.ts
@packages/rts-engine/rts.ts
@packages/rts-engine/index.ts
@apps/web/src/client.ts
@apps/web/src/placement-transform-view-model.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: extract shared union build-zone projection helpers and rewire engine legality usage</name>
  <files>
    packages/rts-engine/build-zone.ts
    packages/rts-engine/build-zone.test.ts
    packages/rts-engine/rts.ts
    packages/rts-engine/index.ts
  </files>
  <action>
    Create a new shared build-zone helper module in `#rts-engine` that encapsulates contributor-center projection and union coverage checks currently embedded in `rts.ts`. Preserve existing authoritative semantics exactly: contributor center from transformed structure anchor plus `floor(width/2)` and `floor(height/2)`, radius and distance shape from gameplay constants, and full-area illegal-cell collection behavior. Replace the local helper implementation in `rts.ts` with imports from the new module so legality checks and future UI overlays depend on one canonical implementation. Add targeted unit tests that lock radius-boundary inclusivity, multi-contributor union behavior, and contributor removal shrink behavior.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/build-zone.test.ts packages/rts-engine/rts.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Build-zone projection math is centralized, authoritative legality still passes existing regressions, and helper tests prove contributor/coverage parity.</done>
</task>

<task type="auto">
  <name>task 2: add pure camera state helpers and deterministic precision tests</name>
  <files>
    apps/web/src/camera-view-model.ts
    tests/web/camera-view-model.test.ts
  </files>
  <action>
    Implement a pure camera helper module for web runtime consumption that manages zoom clamp (`0.45x` to `1.6x`), cursor-anchored zoom, additive pan offsets, keyboard pan steps, and `F` reset targeting (local base center with spectator fallback). Include inverse coordinate conversion helpers that map pointer/canvas positions back into world and grid cell coordinates so future runtime wiring can keep placement and destroy selection precise at every supported zoom level. Add deterministic tests for clamp boundaries, cursor-anchor invariance after zoom operations, pan updates, and reset behavior.
  </action>
  <verify>
    <automated>npx vitest run tests/web/camera-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Camera helper APIs are stable and test-covered, with precise conversion and reset behavior ready for client integration.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/build-zone.test.ts packages/rts-engine/rts.test.ts tests/web/camera-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- UI-02 foundation is true: camera pan/zoom/reset and coordinate conversions are deterministic and bounded to the agreed zoom window.
- UI-05 foundation is true: contributor and coverage math used by legality can be reused directly for visualization with no semantic drift.
- Shared helper modules are exported via `#rts-engine` and protected by focused regression tests.

</success_criteria>

<output>
After completion, create `.planning/phases/11-camera-and-build-zone-visualization/11-01-SUMMARY.md`
</output>
