---
phase: 11-camera-and-build-zone-visualization
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/web/index.html
  - apps/web/src/client.ts
  - apps/web/src/camera-view-model.ts
  - tests/web/camera-view-model.test.ts
autonomous: true
requirements:
  - UI-02
  - UI-05
must_haves:
  truths:
    - 'Players can pan with right-drag and keyboard (WASD/arrows), zoom with wheel/pinch and +/- keys, and press F to reset to base focus.'
    - 'Placement and destroy targeting remain accurate at every supported zoom level because pointer hit-testing uses inverse camera transforms.'
    - 'The local player always sees their union build-zone outline/fill during matches, with stronger emphasis while placing.'
    - 'Zone visuals update immediately on authoritative structure changes and never obscure invalid-cell or placement-ghost feedback.'
  artifacts:
    - path: apps/web/src/client.ts
      provides: Runtime camera input wiring, inverse pointer mapping, and render-pass integration for zone overlays
      contains: right-drag pan handlers, wheel/keyboard zoom controls, reset behavior, authoritative zone recompute and layered drawing
    - path: apps/web/index.html
      provides: In-game viewport shell and camera/zone status presentation surfaces
      contains: camera interaction hint copy, zoom/status text surface, viewport styles supporting transformed canvas rendering
    - path: apps/web/src/camera-view-model.ts
      provides: Runtime-consumed reducers/utilities extended for integration edge cases
      contains: keyboard pan step helpers, zoom factor normalization, reset target helpers used by client event handlers
    - path: tests/web/camera-view-model.test.ts
      provides: Regression coverage for helper behavior used by runtime camera controls
      contains: zoom clamp invariants, cursor-anchor stability, reset targeting, coordinate conversion precision
  key_links:
    - from: apps/web/src/client.ts
      to: apps/web/src/camera-view-model.ts
      via: event handlers and pointer conversion plumbing
      pattern: pointerToCell|applyWheelZoomAtPoint|applyKeyboardPan|resetCameraToBase
    - from: apps/web/src/client.ts
      to: packages/rts-engine/build-zone.ts
      via: authoritative local-team contributor and coverage projection
      pattern: projectBuildZoneContributors|collectIllegalBuildZoneCells
    - from: apps/web/src/client.ts
      to: apps/web/index.html
      via: in-game viewport and camera status bindings
      pattern: grid-viewport|camera-status|camera-zoom
---

<objective>
Integrate pan/zoom controls and always-visible authoritative local-team union-zone rendering into the in-game web canvas without sacrificing placement precision.

Purpose: This plan delivers the user-facing UI-02 and UI-05 outcomes by wiring Phase 11 helper primitives into runtime input handling and layered rendering.
Output: Camera-enabled in-game viewport, cursor-accurate targeting at all supported zoom levels, and live union-zone overlays that update with authoritative structure state.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-camera-and-build-zone-visualization/11-CONTEXT.md
@.planning/phases/11-camera-and-build-zone-visualization/11-RESEARCH.md
@apps/web/index.html
@apps/web/src/client.ts
@apps/web/src/camera-view-model.ts
@tests/web/camera-view-model.test.ts
@packages/rts-engine/build-zone.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add in-game viewport shell and camera status surfaces in web layout</name>
  <files>
    apps/web/index.html
  </files>
  <action>
    Update the in-game board shell to support camera-enabled rendering: add a viewport container around the canvas, include compact camera interaction/status readouts (controls hint + zoom/reset state), and style the board area so panning outside map margins remains visible without clipping required overlay layers. Preserve existing responsive behavior and keep the controls understandable on mobile and desktop. Ensure the board interaction surface suppresses browser context-menu interruption during right-drag camera pan while keeping accessibility semantics intact.
  </action>
  <verify>
    <automated>npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Layout and styles expose a camera-ready viewport, status surfaces, and right-drag-safe interaction shell for in-game board navigation.</done>
</task>

<task type="auto">
  <name>task 2: wire camera input model and precision-safe pointer mapping in client runtime</name>
  <files>
    apps/web/src/client.ts
    apps/web/src/camera-view-model.ts
    tests/web/camera-view-model.test.ts
  </files>
  <action>
    Integrate camera state into `client.ts` so in-match interactions support right-mouse drag pan, WASD/arrow pan, wheel/trackpad pinch zoom, keyboard `+`/`-` zoom, and `F` reset to local base focus (or spectator fallback) with clamp range `0.45x-1.6x`. Keep zoom cursor-anchored by applying offset compensation around the pointer position. Update pointer-to-cell conversion to use inverse camera transforms before grid-cell resolution so placement and destroy targeting remain accurate regardless of zoom or pan offset. Keep drawing and selection behavior compatible with existing gameplay/read-only gating.
  </action>
  <verify>
    <automated>npx vitest run tests/web/camera-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Camera controls are fully wired and pointer-based gameplay targeting remains precise across all supported zoom levels.</done>
</task>

<task type="auto">
  <name>task 3: render authoritative local-team union-zone overlays with placement-priority layering</name>
  <files>
    apps/web/src/client.ts
    apps/web/index.html
  </files>
  <action>
    Use authoritative team structures from state payloads and shared build-zone helpers to compute the local player's union-zone geometry, then render an always-visible fill+outline layer during matches. Apply subtle idle styling and stronger placement emphasis while template mode is active. Refresh zone geometry immediately on authoritative structure updates (including build apply and destroy outcomes) with no transition animation. Enforce render-pass ordering so zone visuals stay above terrain/grid but below structure/preview overlays, and keep invalid placement cells plus placement ghost as highest visual-priority cues.
  </action>
  <verify>
    <automated>npx vitest run tests/web/camera-view-model.test.ts tests/web/placement-transform-view-model.test.ts && npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Local-team union-zone visuals remain visible and accurate through authoritative updates, with clear idle-vs-placing emphasis and preserved preview feedback priority.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/camera-view-model.test.ts tests/web/placement-transform-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- UI-02 is true: pan/zoom/reset controls work from the locked input set without placement or destroy targeting drift.
- UI-05 is true: local-team union build-zone outline/fill is visible during matches and strengthens during placement mode.
- Zone updates redraw instantly from authoritative structure changes and preserve invalid-cell/ghost top visual priority.

</success_criteria>

<output>
After completion, create `.planning/phases/11-camera-and-build-zone-visualization/11-02-SUMMARY.md`
</output>
