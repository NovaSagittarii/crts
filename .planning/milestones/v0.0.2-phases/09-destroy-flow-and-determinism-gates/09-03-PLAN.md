---
phase: 09-destroy-flow-and-determinism-gates
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/web/index.html
  - apps/web/src/client.ts
  - apps/web/src/destroy-view-model.ts
  - tests/web/destroy-view-model.test.ts
autonomous: true
requirements:
  - STRUCT-02
  - QUAL-04
must_haves:
  truths:
    - 'Destroy controls appear only after selecting an owned structure and remain hidden for non-owned targets.'
    - 'Base structures require inline confirmation before destroy queue submission, driven by authoritative structure metadata.'
    - 'Destroy requests show explicit pending state, preserve retargeting behavior, and keep duplicate same-target submissions idempotent.'
    - 'Accepted and rejected destroy outcomes provide inline plus toast feedback for the acting player without opponent-only destroy notices.'
    - 'After reconnect, the client shows a subtle synced notice and rebuilds destroy interaction state from authoritative payloads.'
  artifacts:
    - path: 'apps/web/index.html'
      provides: 'Destroy action surfaces in the existing gameplay panel'
      contains: 'destroy action button, base confirm controls, pending and rejection status slots'
    - path: 'apps/web/src/destroy-view-model.ts'
      provides: 'Deterministic client destroy interaction state transitions'
      contains: 'selection ownership gate, pending target tracking, confirmation requirements, outcome-state transitions'
    - path: 'apps/web/src/client.ts'
      provides: 'Socket destroy queue and outcome wiring with reconnect synced UX'
      contains: 'structure selection plumbing, destroy queue emit, queued and outcome handlers, reconnect synced notice'
    - path: 'tests/web/destroy-view-model.test.ts'
      provides: 'Deterministic regression coverage for destroy interaction flow'
      contains: 'owned vs non-owned selection, base confirmation gate, pending idempotency, reset on success'
  key_links:
    - from: 'apps/web/index.html'
      to: 'apps/web/src/client.ts'
      via: 'destroy action control ids and status surfaces'
      pattern: 'destroy|confirm|pending'
    - from: 'apps/web/src/client.ts'
      to: 'packages/rts-engine/socket-contract.ts'
      via: 'destroy queue and outcome payload usage'
      pattern: 'DestroyQueuePayload|DestroyOutcomePayload|destroy:queue'
    - from: 'apps/web/src/destroy-view-model.ts'
      to: 'tests/web/destroy-view-model.test.ts'
      via: 'state transition regression tests'
      pattern: 'requiresConfirm|pending|idempotent'
---

<objective>
Deliver in-match destroy controls in the web client with authoritative pending, confirmation, rejection, and reconnect feedback behavior.

Purpose: Complete player-facing STRUCT-02 behavior while preserving QUAL-04 parity and reconnect convergence expectations.
Output: Destroy interaction surfaces, client state helpers, and web regression coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-CONTEXT.md
@.planning/phases/09-destroy-flow-and-determinism-gates/09-RESEARCH.md
@apps/web/index.html
@apps/web/src/client.ts
@tests/web/economy-view-model.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add owned-structure selection and destroy controls with confirmation state</name>
  <files>
    apps/web/index.html
    apps/web/src/destroy-view-model.ts
    apps/web/src/client.ts
    tests/web/destroy-view-model.test.ts
  </files>
  <action>
    Add destroy action controls to the gameplay panel and implement a dedicated destroy view-model to track selected structure, ownership visibility, confirmation requirement, and pending target state. Render destroy controls only for owned structures and require explicit inline confirmation when authoritative structure metadata marks the target as confirmation-required. Keep pending state visible and allow retargeting while preventing duplicate same-target submissions from producing duplicate local pending rows.
  </action>
  <verify>
    <automated>npx vitest run tests/web/destroy-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Client destroy controls are deterministic, ownership-gated, and confirmation-aware with test-backed state transitions.</done>
</task>

<task type="auto">
  <name>task 2: wire destroy socket events, pending and rejection feedback, and reconnect synced notice</name>
  <files>
    apps/web/index.html
    apps/web/src/client.ts
  </files>
  <action>
    Wire destroy queue submission and handlers for destroy queued and terminal outcome payloads. Show subtle success and rejection feedback to the acting player in both inline and toast surfaces, and clear destroy-ready state after accepted destroy. Keep opponent UX state-driven with no dedicated destroy notice channel. After reconnect and first authoritative state resync, show a non-blocking `Reconnected, state synced` notice and rebuild destroy interaction state from authoritative payloads.
  </action>
  <verify>
    <automated>npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Web destroy UX aligns with authoritative runtime outcomes, pending flow, and reconnect-safe state synchronization expectations.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/destroy-view-model.test.ts tests/web/economy-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- STRUCT-02 is true in UI: players can select owned structures, queue destroy actions, and receive authoritative outcome feedback.
- Base-destroy confirmation is data-driven from authoritative structure metadata.
- QUAL-04 UX gate is true: reconnect displays synced notice and restore-from-state behavior without client divergence.
  </success_criteria>

<output>
After completion, create `.planning/phases/09-destroy-flow-and-determinism-gates/09-03-SUMMARY.md`
</output>
