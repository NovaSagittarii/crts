---
phase: 06-base-geometry-and-integrity-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rts-engine/geometry.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - packages/rts-engine/spawn.test.ts
autonomous: true
requirements:
  - BASE-01
must_haves:
  truths:
    - 'New matches seed the same canonical 5x5 base footprint with exactly 16 occupied base cells.'
    - 'Territory center, spawn spacing, and base-related checks all use the 5x5 contract (center offset +2).'
    - 'Breach-pressure fixtures target the canonical footprint instead of legacy 2x2 assumptions.'
  artifacts:
    - path: 'packages/rts-engine/geometry.ts'
      provides: 'Shared 5x5 base footprint helper/model'
      contains: 'corner 2x2 occupancy predicate and center helper'
    - path: 'packages/rts-engine/rts.ts'
      provides: 'Engine wiring for base seeding, territory center math, and spawn footprint checks'
      contains: 'helper-driven base logic (no hardcoded +1 center math)'
    - path: 'packages/rts-engine/rts.test.ts'
      provides: 'Unit tests proving canonical base shape + territory semantics'
      contains: '5x5 shape assertions with 16 occupied cells'
  key_links:
    - from: 'packages/rts-engine/rts.ts'
      to: 'packages/rts-engine/geometry.ts'
      via: 'base footprint helper usage in seeding/territory logic'
      pattern: 'seedBase|countTerritoryCells|isTeamTerritoryPlacementValid'
    - from: 'packages/rts-engine/rts.ts'
      to: 'packages/rts-engine/spawn.ts'
      via: 'spawn layout configuration'
      pattern: 'createTorusSpawnLayout.*baseWidth.*5'
---

<objective>
Canonicalize base geometry so all engine computations use the locked 5x5 footprint contract.

Purpose: Remove legacy 2x2/+1 assumptions that would invalidate breach pressure, spawn separation, and territory checks in v0.0.2.
Output: Shared footprint helpers plus updated engine and unit tests that enforce BASE-01.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-base-geometry-and-integrity-core/06-CONTEXT.md
@.planning/phases/02-match-lifecycle-breach-outcomes/02-01-SUMMARY.md
@.planning/phases/03-deterministic-build-queue-validation/03-01-SUMMARY.md
@packages/rts-engine/geometry.ts
@packages/rts-engine/rts.ts
@packages/rts-engine/spawn.ts
@packages/rts-engine/rts.test.ts
@packages/rts-engine/spawn.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: implement canonical 5x5 base helper and engine wiring</name>
  <files>
    packages/rts-engine/geometry.ts
    packages/rts-engine/rts.ts
  </files>
  <action>
    Introduce a shared base-footprint helper/model that encodes the locked 5x5 corner-block occupancy (`##.## / ##.## / ..... / ##.## / ##.##`) and base-center formula (`baseTopLeft + 2`). Then replace legacy 2x2 and `+1` center assumptions in engine code paths with helper-driven logic: base seeding, territory-center calculations, territory placement validation, and spawn overlap checks. Keep spawn generator deterministic by passing explicit footprint dimensions (5x5) and minimum wrapped distance derived as `baseWidth + 1`; do not alter spawn randomness/orientation algorithms.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/spawn.test.ts packages/rts-engine/rts.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Engine no longer relies on hardcoded 2x2/+1 base assumptions; all base-related geometry paths route through the shared helper and 5x5 contract.</done>
</task>

<task type="auto">
  <name>task 2: lock BASE-01 with deterministic unit fixtures</name>
  <files>
    packages/rts-engine/rts.test.ts
    packages/rts-engine/spawn.test.ts
  </files>
  <action>
    Update/add tests to assert canonical base behavior directly: exactly 16 occupied base cells in the 5x5 footprint, empty center cross cells, and territory checks derived from center offset `+2`. Replace legacy breach/placement fixtures that assume a contiguous 2x2 base with footprint-aware fixtures so breach-pressure scenarios target canonical occupied cells only. Keep assertions deterministic and requirement-traceable to BASE-01.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Unit tests fail on any regression from the canonical 5x5 footprint contract and pass when the engine honors BASE-01.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts` passes.
- `npm run test:unit` passes without introducing non-deterministic order regressions.
</verification>

<success_criteria>

- New room setup always seeds a 5x5 base footprint with 16 occupied cells using the shared helper contract.
- Territory center math and spawn spacing are computed from the canonical 5x5 footprint (`center = topLeft + 2`, `minWrappedDistance = baseWidth + 1`).
- BASE-01 unit coverage catches legacy 2x2 assumptions and verifies canonical breach-pressure fixture behavior.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-base-geometry-and-integrity-core/06-01-SUMMARY.md`
</output>
