---
phase: 12-structure-hover-and-tactical-overlays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/structure-interaction-view-model.ts
  - apps/web/src/tactical-overlay-view-model.ts
  - tests/web/structure-interaction-view-model.test.ts
  - tests/web/tactical-overlay-view-model.test.ts
autonomous: true
requirements:
  - UI-03
  - UI-04
must_haves:
  truths:
    - 'Hover and pin selection transitions are deterministic: one active structure key, leave grace support, and explicit pin/unpin behavior.'
    - 'Tactical overlay sections (Economy, Build, Team) can be derived from authoritative payload snapshots with key-first summaries and expandable detail rows.'
    - 'Overlay sync hints, delta highlights, and pending action badges can be projected from typed helper outputs without client-side simulation assumptions.'
  artifacts:
    - path: apps/web/src/structure-interaction-view-model.ts
      provides: Pure structure hover/pin interaction reducer helpers
      contains: single-active-key semantics, leave-grace expiry handling, pinned-key reconciliation hooks
    - path: apps/web/src/tactical-overlay-view-model.ts
      provides: Pure tactical section projection helpers for Economy/Build/Team panels
      contains: summary/detail projection, sync-hint gating, and one-second delta-highlight metadata
    - path: tests/web/structure-interaction-view-model.test.ts
      provides: Deterministic regression coverage for hover/pin edge cases
      contains: grace timeout behavior, pin persistence, re-entry cancellation, missing-structure reconciliation
    - path: tests/web/tactical-overlay-view-model.test.ts
      provides: Deterministic projection coverage for tactical overlay state
      contains: authoritative summary mapping, delta highlight expiration, pending badge projection
  key_links:
    - from: tests/web/structure-interaction-view-model.test.ts
      to: apps/web/src/structure-interaction-view-model.ts
      via: reducer transition assertions
      pattern: createStructureInteractionState|reduceStructureInteraction
    - from: tests/web/tactical-overlay-view-model.test.ts
      to: apps/web/src/tactical-overlay-view-model.ts
      via: projection output and timing assertions
      pattern: deriveTacticalOverlayState|createTacticalOverlayState
---

<objective>
Create deterministic helper foundations for structure inspection and tactical overlay projection before wiring runtime DOM and canvas interactions.

Purpose: UI-03 and UI-04 require nuanced interaction timing and projection behavior that should be unit-tested in pure modules to avoid `client.ts` drift.
Output: New pure view-model helpers plus focused tests for hover/pin timing, section projection, sync hints, and delta highlight behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-structure-hover-and-tactical-overlays/12-CONTEXT.md
@.planning/phases/12-structure-hover-and-tactical-overlays/12-RESEARCH.md
@apps/web/src/client.ts
@apps/web/src/destroy-view-model.ts
@apps/web/src/economy-view-model.ts
@tests/web/destroy-view-model.test.ts
@tests/web/economy-view-model.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: implement deterministic structure hover and pin interaction helpers</name>
  <files>
    apps/web/src/structure-interaction-view-model.ts
    tests/web/structure-interaction-view-model.test.ts
  </files>
  <action>
    Create a pure helper module that models structure interaction state as reducer transitions for hover enter, hover leave (with grace timeout target around 300ms), pointer re-entry, pin on click/tap, unpin, and authoritative structure removal. Keep one active structure at a time and ensure hover-only state never exposes actionable controls. Include utility selectors that runtime code can consume without duplicating transition rules in `client.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/web/structure-interaction-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Structure interaction transitions are deterministic and test-covered for grace delay, pin persistence, unpin behavior, and authoritative invalidation paths.</done>
</task>

<task type="auto">
  <name>task 2: implement tactical overlay projection helpers for economy build and team sections</name>
  <files>
    apps/web/src/tactical-overlay-view-model.ts
    tests/web/tactical-overlay-view-model.test.ts
  </files>
  <action>
    Create a pure overlay projection module that maps authoritative team/template/pending data into section-ready structures for Economy, Build, and Team displays. Include compact summary-first outputs, expandable detail rows, one-second delta highlight metadata, optional syncing-hint state, and optimistic pending badge projections for queued actions. Preserve player-friendly message shaping by accepting already-formatted reason copy inputs from runtime consumers.
  </action>
  <verify>
    <automated>npx vitest run tests/web/tactical-overlay-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Overlay projection helpers provide stable section data and timing metadata, with deterministic tests covering summary mapping, pending cues, and highlight expiry behavior.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/structure-interaction-view-model.test.ts tests/web/tactical-overlay-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- UI-03 foundation is true: hover/pin interaction semantics are explicit, deterministic, and reusable.
- UI-04 foundation is true: tactical section projection and timing cues are centralized in pure helpers.
- Runtime integration can consume helper outputs without re-implementing hover timing or section mapping logic.

</success_criteria>

<output>
After completion, create `.planning/phases/12-structure-hover-and-tactical-overlays/12-01-SUMMARY.md`
</output>
