---
phase: 12-structure-hover-and-tactical-overlays
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - apps/web/index.html
  - apps/web/src/client.ts
  - apps/web/src/structure-interaction-view-model.ts
  - apps/web/src/tactical-overlay-view-model.ts
autonomous: true
requirements:
  - UI-03
  - UI-04
must_haves:
  truths:
    - 'Hovering structures shows a stable ephemeral inspector preview, while clicks/taps pin one structure for actionable interaction.'
    - 'Action controls only appear for the pinned structure, and queue feedback appears near the inspector/overlay source with optimistic pending cues followed by authoritative outcomes.'
    - 'Players can access Economy, Build, and Team tactical overlays adjacent to the grid on desktop and through a tabbed drawer on small screens.'
    - 'Overlay and inspector data stay synchronized with authoritative state/build/destroy updates, including delta highlights and delayed-data syncing hints.'
  artifacts:
    - path: apps/web/index.html
      provides: Grid-adjacent tactical UI layout for desktop rail and mobile tabbed drawer sections
      contains: structure inspector surface plus Economy/Build/Team section containers and responsive tab controls
    - path: apps/web/src/client.ts
      provides: Runtime hover/pin wiring, inspector rendering, tactical section projection, and action feedback synchronization
      contains: structure interaction reducer usage, overlay render/update functions, authoritative payload synchronization hooks
    - path: apps/web/src/structure-interaction-view-model.ts
      provides: Runtime-consumed hover/pin helpers tuned by implementation feedback
      contains: grace timeout controls and selector helpers used by client pointer handlers
    - path: apps/web/src/tactical-overlay-view-model.ts
      provides: Runtime-consumed tactical projection helpers tuned for final section rendering
      contains: section projection and highlight metadata consumed by DOM update routines
  key_links:
    - from: apps/web/src/client.ts
      to: apps/web/src/structure-interaction-view-model.ts
      via: pointer move/click transitions and pin state selectors
      pattern: reduceStructureInteraction|selectPinnedStructure
    - from: apps/web/src/client.ts
      to: apps/web/src/tactical-overlay-view-model.ts
      via: authoritative payload projection into Economy Build Team section rendering
      pattern: deriveTacticalOverlayState|renderTacticalOverlay
    - from: apps/web/src/client.ts
      to: apps/web/index.html
      via: inspector and tactical section DOM bindings
      pattern: structure-inspector|tactical-overlay|overlay-tab
---

<objective>
Ship the full in-game structure inspector and tactical overlay experience by wiring hover/pin interactions, adjacent overlay surfaces, and authoritative action feedback into the current web runtime.

Purpose: This plan completes UI-03 and UI-04 outcomes on top of Phase 11 camera precision and Phase 12 helper foundations.
Output: Responsive tactical overlay layout and runtime synchronization that keeps inspector details/actions aligned with authoritative match updates.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-structure-hover-and-tactical-overlays/12-CONTEXT.md
@.planning/phases/12-structure-hover-and-tactical-overlays/12-RESEARCH.md
@.planning/phases/12-structure-hover-and-tactical-overlays/12-01-PLAN.md
@apps/web/index.html
@apps/web/src/client.ts
@apps/web/src/destroy-view-model.ts
@apps/web/src/economy-view-model.ts
@apps/web/src/structure-interaction-view-model.ts
@apps/web/src/tactical-overlay-view-model.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add desktop tactical rail and mobile tabbed drawer surfaces adjacent to the grid</name>
  <files>
    apps/web/index.html
  </files>
  <action>
    Update the in-game layout so tactical information lives next to the board instead of only in the left control block. Add a persistent desktop side rail with collapsible Economy/Build/Team sections and a dedicated structure inspector panel. Add a mobile tabbed drawer variant for the same three sections with compact toggles, preserving board visibility and existing responsive breakpoints. Keep styling aligned with current visual language and include subtle containers for delta highlights, syncing hints, pending badges, and inline feedback copy.
  </action>
  <verify>
    <automated>npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>HTML/CSS provide all inspector and tactical section surfaces for desktop and mobile without regressing in-match board visibility.</done>
</task>

<task type="auto">
  <name>task 2: wire structure hover preview and click to pin interactions into camera-aware board input flow</name>
  <files>
    apps/web/src/client.ts
    apps/web/src/structure-interaction-view-model.ts
  </files>
  <action>
    Integrate the structure interaction helper into pointer handling so hover updates follow camera-aware `pointerToCell` mapping, apply leave grace delay, and keep a single active structure. Click/tap should pin or unpin the structure and drive inspector rendering state. Ensure actionable controls are hidden for hover-only states and shown only when pinned. Reconcile pinned/hovered keys during authoritative structure refreshes so removed structures clear automatically. Keep existing paint/template/destroy action flows intact while routing pinned structure actions through existing queue emit paths.
  </action>
  <verify>
    <automated>npx vitest run tests/web/structure-interaction-view-model.test.ts tests/web/destroy-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Inspector behavior matches locked interaction rules with deterministic hover grace and pinned-only actions while preserving existing gameplay mutation gates.</done>
</task>

<task type="auto">
  <name>task 3: synchronize tactical sections and source-local action feedback with authoritative updates</name>
  <files>
    apps/web/src/client.ts
    apps/web/src/tactical-overlay-view-model.ts
  </files>
  <action>
    Drive Economy/Build/Team section rendering from authoritative state payloads plus existing preview/queued/outcome channels using tactical overlay projection helpers. Show key metrics first with expandable details, apply one-second delta highlight cues on changed values, and show syncing hints only when reconnect/delay conditions indicate stale data risk. Display optimistic pending badges immediately after queue actions, then replace them with authoritative queued/outcome copy near the inspector or section where action originated. Keep reason phrasing player-friendly by reusing existing build/destroy reason formatters.
  </action>
  <verify>
    <automated>npx vitest run tests/web/tactical-overlay-view-model.test.ts tests/web/economy-view-model.test.ts tests/web/placement-transform-view-model.test.ts && npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Tactical overlays and inspector feedback remain authoritative, responsive, and readable across normal play, reconnect windows, and queue action outcomes.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/structure-interaction-view-model.test.ts tests/web/tactical-overlay-view-model.test.ts tests/web/destroy-view-model.test.ts tests/web/economy-view-model.test.ts tests/web/placement-transform-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- UI-03 is true: hover inspection works with flicker-resistant grace timing, and pinned structures expose actionable controls.
- UI-04 is true: Economy/Build/Team tactical overlays are accessible adjacent to the grid on desktop and mobile.
- Overlay and inspector values stay synchronized with authoritative match updates and expose local pending plus final outcome feedback clearly.

</success_criteria>

<output>
After completion, create `.planning/phases/12-structure-hover-and-tactical-overlays/12-02-SUMMARY.md`
</output>
