---
phase: 07-authoritative-union-build-zones
plan: 01
type: execute
wave: 1
depends_on:
  - 06-02
files_modified:
  - packages/rts-engine/gameplay-rules.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/rts.test.ts
  - apps/server/src/server.ts
  - apps/web/src/client.ts
  - tests/integration/server/server.test.ts
  - tests/integration/server/quality-gate-loop.test.ts
autonomous: true
requirements:
  - BUILD-01
  - BUILD-02
must_haves:
  truths:
    - 'Queue legality is determined by the union of radius-15 zones contributed by owned placed structures with HP > 0.'
    - 'Placement is accepted only when every footprint cell is inside the union zone (inclusive boundary).'
    - 'Queued events are revalidated at authoritative resolve time using current zone contributors.'
    - 'Out-of-zone rejection reason remains deterministic and client feedback uses the build-zone wording.'
  artifacts:
    - path: 'packages/rts-engine/rts.ts'
      provides: 'Authoritative union-zone legality checks in queue and resolve paths'
      contains: 'full-footprint radius checks, post-apply contributor activation, deterministic occupied-site guard'
    - path: 'packages/rts-engine/gameplay-rules.ts'
      provides: 'Shared union-zone radius constants'
      contains: 'BUILD_ZONE_RADIUS and distance-shape config'
    - path: 'packages/rts-engine/rts.test.ts'
      provides: 'BUILD-01/BUILD-02 unit coverage'
      contains: 'inclusive boundary, footprint overflow, expansion/shrink semantics'
    - path: 'tests/integration/server/server.test.ts'
      provides: 'Server queue/preview contract coverage under union-zone rules'
      contains: 'valid placement helpers aligned with full-footprint zone checks'
---

<objective>
Enforce authoritative union build-zone placement legality with fixed radius `15` while preserving deterministic queue and outcome behavior.

Purpose: Replace legacy territory-center checks with structure-contributed union checks so placement eligibility expands/shrinks from authoritative structure lifecycle events.
Output: Engine legality updates, runtime feedback wording alignment, and unit/integration coverage for BUILD-01 and BUILD-02.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-authoritative-union-build-zones/07-CONTEXT.md
@packages/rts-engine/gameplay-rules.ts
@packages/rts-engine/rts.ts
@packages/rts-engine/rts.test.ts
@apps/server/src/server.ts
@apps/web/src/client.ts
@tests/integration/server/server.test.ts
@tests/integration/server/quality-gate-loop.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: replace territory-center placement checks with union build-zone legality</name>
  <files>
    packages/rts-engine/gameplay-rules.ts
    packages/rts-engine/rts.ts
  </files>
  <action>
    Add fixed union-zone radius constants and implement structure-contributor zone checks in the authoritative engine. Validate full placement footprints against the radius-15 union (inclusive edge) and revalidate due queue events against current contributors. Ensure new structures contribute only after authoritative apply completion, and destroyed structures (HP <= 0) stop contributing immediately.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Placement legality no longer depends on team territory-center radius; queue and resolve paths share deterministic union-zone checks.</done>
</task>

<task type="auto">
  <name>task 2: align server/client feedback and deterministic integration fixtures</name>
  <files>
    apps/server/src/server.ts
    apps/web/src/client.ts
    tests/integration/server/server.test.ts
    tests/integration/server/quality-gate-loop.test.ts
  </files>
  <action>
    Align rejection messaging to build-zone copy and keep a stable reason code. Add short cooldown dedupe for repeated client-side invalid build toasts. Update integration placement helpers so candidate coordinates satisfy full-footprint union-zone legality, and stabilize queue timing in quality-gate breach loops under varying spawn seeds.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Server/web feedback and integration suites reflect authoritative union-zone behavior without nondeterministic queue regressions.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts` passes.
- `npx vitest run tests/integration/server/server.test.ts tests/integration/server/match-lifecycle.test.ts tests/integration/server/quality-gate-loop.test.ts` passes.
- `npm run test:quality` passes.
</verification>

<success_criteria>

- BUILD-01 is true: queue/build legality accepts only placements whose full footprint is inside union coverage from owned structures.
- BUILD-02 is true: union-zone checks use radius `15` with inclusive boundary semantics.
- Zone expansion/shrink is authoritative and deterministic across structure apply and structure destruction flows.
- Queue/preview/server feedback and integration tests remain deterministic across varying spawn seeds.
  </success_criteria>

<output>
After completion, create `.planning/phases/07-authoritative-union-build-zones/07-01-SUMMARY.md`
</output>
