---
phase: 08-transform-placement-consistency
plan: 01
type: execute
wave: 1
depends_on:
  - 07-01
files_modified:
  - packages/rts-engine/placement-transform.ts
  - packages/rts-engine/rts.ts
  - packages/rts-engine/socket-contract.ts
  - packages/rts-engine/index.ts
  - packages/rts-engine/rts.test.ts
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - QUAL-03
must_haves:
  truths:
    - Rotate and mirror transforms are represented once with order-sensitive composition and reused across preview, queue, and simulation paths.
    - Edge crossing is legal through torus wrapping; transformed placement is rejected only when transformed dimensions exceed room dimensions.
    - Accepted transformed placements persist normalized transform metadata so apply and integrity checks use the same footprint/orientation the player previewed.
  artifacts:
    - path: packages/rts-engine/placement-transform.ts
      provides: Canonical transform composition and wrapped footprint projection
      contains: matrix-based transform state, ordered operations, map-size guard
    - path: packages/rts-engine/rts.ts
      provides: Transform-aware placement validation plus apply/integrity parity
      contains: shared validator for preview/queue/resolve and persisted transform metadata on events/structures
    - path: packages/rts-engine/socket-contract.ts
      provides: Transform-aware build preview/queue contract fields
      contains: transform payload, projected footprint payload, and template-exceeds-map-size reason support
    - path: packages/rts-engine/rts.test.ts
      provides: Deterministic transform placement coverage
      contains: rotate/mirror order tests, torus wrap cases, and map-size rejection assertions
  key_links:
    - from: packages/rts-engine/placement-transform.ts
      to: packages/rts-engine/rts.ts
      via: shared projection helpers used by validation and apply
      pattern: compose.*transform|project.*footprint
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/socket-contract.ts
      via: build request/response typing
      pattern: BuildQueuePayload|BuildPreviewPayload
    - from: packages/rts-engine/rts.ts
      to: packages/rts-engine/rts.test.ts
      via: legality and parity regression assertions
      pattern: template-exceeds-map-size|outside-territory
---

<objective>
Create one authoritative transform pipeline in the engine so rotate/mirror placement behavior is deterministic and parity-safe before runtime/UI wiring.

Purpose: Prevent preview/queue/apply/integrity drift by centralizing transform math and legality semantics in `packages/rts-engine`.
Output: New transform utility module, transform-aware contracts, queue/apply/integrity wiring, and unit coverage for XFORM-01/XFORM-02/QUAL-03 foundations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-transform-placement-consistency/08-CONTEXT.md
@.planning/phases/08-transform-placement-consistency/08-RESEARCH.md
@.planning/phases/07-authoritative-union-build-zones/07-01-SUMMARY.md
@packages/rts-engine/rts.ts
@packages/rts-engine/socket-contract.ts
@packages/rts-engine/index.ts
@packages/rts-engine/rts.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: add canonical transform model and shared payload surfaces</name>
  <files>
    packages/rts-engine/placement-transform.ts
    packages/rts-engine/socket-contract.ts
    packages/rts-engine/index.ts
    packages/rts-engine/rts.test.ts
  </files>
  <action>
    Create `placement-transform.ts` with a composable matrix-backed transform model that preserves ordered user operations (`rotate`, `mirror-horizontal`, `mirror-vertical`) and supports no-op/symmetric transform indicators. Use this model because user decisions lock order-sensitive behavior and matrix representation. Extend queue/preview contract types to carry transform state and to return projected footprint + illegal-cell coordinates + bounding box metadata and reason taxonomy, including explicit `template-exceeds-map-size`. Add unit tests that pin rotate cycle behavior (`0 -> 90 -> 180 -> 270 -> 0`), separate mirror controls, and order-sensitive composition outcomes.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Transform composition utilities and wire contracts exist, and tests lock ordered rotate/mirror semantics plus explicit map-size reason support.</done>
</task>

<task type="auto">
  <name>task 2: route queue, resolve, apply, and integrity through transform-aware validation</name>
  <files>
    packages/rts-engine/placement-transform.ts
    packages/rts-engine/rts.ts
    packages/rts-engine/rts.test.ts
  </files>
  <action>
    Replace axis-aligned `inBounds` legality for placement with a shared transform-aware validator that uses transformed-size guard + torus-wrapped footprint projection before union-zone checks. Keep `outside-territory` behavior intact for zone failures, but reject oversize transformed templates with `template-exceeds-map-size`. Persist normalized transform metadata on queued events and stored structure instances, apply templates using projected transformed occupied cells, and run integrity checks against transformed footprints to keep applied outcomes consistent with preview orientation.
  </action>
  <verify>
    <automated>npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Preview/queue/apply/integrity paths use one transformed footprint pipeline, and transformed placements remain deterministic across identical runs.</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/rts-engine/rts.test.ts packages/rts-engine/spawn.test.ts` passes.
- `npm run test:unit` passes.
</verification>

<success_criteria>

- XFORM-01 foundation is in place: engine model supports deterministic rotate cycle inputs before queue/apply.
- XFORM-02 foundation is in place: horizontal and vertical mirror operations compose with rotation in press-order semantics.
- QUAL-03 engine parity holds: transformed legality/apply/integrity use the same projected footprint and explicit reason taxonomy.
  </success_criteria>

<output>
After completion, create `.planning/phases/08-transform-placement-consistency/08-01-SUMMARY.md`
</output>
