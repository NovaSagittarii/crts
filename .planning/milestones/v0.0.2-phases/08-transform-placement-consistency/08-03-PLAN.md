---
phase: 08-transform-placement-consistency
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/web/index.html
  - apps/web/src/client.ts
  - apps/web/src/placement-transform-view-model.ts
  - tests/web/placement-transform-view-model.test.ts
autonomous: true
requirements:
  - XFORM-01
  - XFORM-02
  - QUAL-03
must_haves:
  truths:
    - Player can rotate templates with cycle `0 -> 90 -> 180 -> 270 -> 0` and mirror horizontally/vertically, with explicit indicators that preserve press-order semantics.
    - Preview shows transformed occupied cells plus bounding box, highlights partial illegality (illegal cells red), and shows short reason labels so legality is not color-only.
    - Queue action stays disabled while preview is invalid, queue rejection keeps anchor+transform for quick adjustment, queue success keeps preview active and transform persisted for repeated placements, and player can explicitly exit via Cancel Build Mode.
  artifacts:
    - path: apps/web/index.html
      provides: Transform controls and status surfaces in build panel
      contains: rotate/mirror buttons, transform indicator, cancel build mode action, confirmation/reason UI slots
    - path: apps/web/src/placement-transform-view-model.ts
      provides: Client transform-state helpers for ordered operations and indicator formatting
      contains: rotate cycle helper, mirror toggles, matrix-normalization bridge, no-op state updates
    - path: apps/web/src/client.ts
      provides: Authoritative preview rendering and queue UX parity wiring
      contains: transformed footprint drawing, illegal-cell highlighting, reason labels, queue disable gating, reconcile-on-reject refresh handling
    - path: tests/web/placement-transform-view-model.test.ts
      provides: Deterministic transform UX state regression coverage
      contains: rotate cycle, order sensitivity, persistent transform, and no-op indicator assertions
  key_links:
    - from: apps/web/index.html
      to: apps/web/src/client.ts
      via: transform control element IDs and click handlers
      pattern: rotate|mirror|cancel-build-mode
    - from: apps/web/src/client.ts
      to: packages/rts-engine/socket-contract.ts
      via: build preview/queue payload and response usage
      pattern: BuildPreviewPayload|BuildPreviewRequestPayload
    - from: apps/web/src/client.ts
      to: tests/web/placement-transform-view-model.test.ts
      via: shared transform state helper imports
      pattern: placement-transform-view-model
---

<objective>
Deliver rotate/mirror placement UX in the web client while keeping preview/queue behavior aligned with authoritative legality and outcome signals.

Purpose: Complete player-facing XFORM controls and parity feedback so transformed placement feels fast, clear, and deterministic.
Output: Build-panel transform controls, transformed preview rendering with reason labels, queue gating/reconciliation UX, and web regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-transform-placement-consistency/08-CONTEXT.md
@.planning/phases/08-transform-placement-consistency/08-RESEARCH.md
@apps/web/index.html
@apps/web/src/client.ts
@tests/web/economy-view-model.test.ts
</context>

<tasks>

<task type="auto">
  <name>task 1: implement persistent transform state helpers and build-panel controls</name>
  <files>
    apps/web/index.html
    apps/web/src/placement-transform-view-model.ts
    apps/web/src/client.ts
    tests/web/placement-transform-view-model.test.ts
  </files>
  <action>
    Add explicit rotate, mirror-horizontal, mirror-vertical, and Cancel Build Mode controls to the build panel with mobile-safe layout behavior. Implement client transform state as ordered operations plus normalized matrix bridge so repeated presses (including symmetric/no-op states) still update indicator state. Wire rotate to the required cycle order and keep transform state persistent until manual changes or explicit build-mode cancel; do not auto-reset after queue success/rejection.
  </action>
  <verify>
    <automated>npx vitest run tests/web/placement-transform-view-model.test.ts</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Build panel exposes rotate/mirror/cancel controls and tested transform state helpers match locked order-sensitive semantics.</done>
</task>

<task type="auto">
  <name>task 2: render transformed preview legality and align queue feedback behavior</name>
  <files>
    apps/web/index.html
    apps/web/src/client.ts
  </files>
  <action>
    Replace bounding-box-only preview rendering with authoritative transformed footprint rendering from server preview payloads, including bounding box, per-cell coloring (illegal red, legal normal), and short reason labels that match queue rejection taxonomy. Keep queue disabled when preview is invalid and surface reason copy inline. After rejection, keep current anchor+transform and show authoritative reason while immediately consuming refreshed legality payload; after acceptance, keep preview active/transform persisted and show a small non-blocking confirmation while queue panel state updates.
  </action>
  <verify>
    <automated>npm run build</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Web preview and queue UX satisfy XFORM/QUAL parity decisions, including partial-illegal rendering, consistent reasons, persistent transform flow, and explicit build-mode exit.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/web/placement-transform-view-model.test.ts tests/web/economy-view-model.test.ts` passes.
- `npm run build` passes.
</verification>

<success_criteria>

- XFORM-01 is true in UI: rotate control cycles through all four orientations before preview and queue.
- XFORM-02 is true in UI: horizontal and vertical mirror controls affect preview and queue payloads with order-sensitive behavior.
- QUAL-03 is true in UI: preview legality labels/cell highlights match queue outcomes and preserve authoritative reconciliation without dropping placement context.
  </success_criteria>

<output>
After completion, create `.planning/phases/08-transform-placement-consistency/08-03-SUMMARY.md`
</output>
